# 小红书工作流升级方案（v2 · Agent 可执行版）

> 日期：2026-07-14（v2，当日二次实测调研后修订；v2.1 同日增补 §12 代码规范、§13 文件精简清单与 T11/T12，目标：升级完成后同步为公开 GitHub 项目）
> 目的：在保留现有小红书工作流资产的前提下完成一次「削薄」升级——发布链移出主流程、配图改造为可插拔 skill 路由、研究与成图各增一道复核闸门；随后完成仓库化，公开到 GitHub。
> **本文档面向执行 Agent**：§7 任务清单给出精确到文件的改动规格与验收标准，可直接按 T1→T12 顺序落地。

## 1. 结论（一段话）

把当前「九节点成稿 + MCP 发布」改造成：**只读研究引擎 + 技能化视觉引擎 + 双闸门复核 + 人工发布封装**。不重做骨架（run 目录 / run.yaml / verify.mjs 全保留），只重排风险与职责。

## 2. 用户输入约束（不可违背）

1. 出于封号风险考虑，**不再通过 MCP 自动发布**。
2. 小红书 MCP **仅保留给选题阶段查找图文**（兼容备用）。
3. 配图阶段内置多个 skill，**可手动指定，也可自动选择**。
4. 选题探索与配图生成后，**均增加专门复核工序**。
5. 充分参考 GitHub 上现成可复用项目 / skill。
6. 红线不变：**不做自动评论 / 点赞 / 收藏 / 互动**。

## 3. 现状快照（2026-07-14 实测，改动前基线）

| 资产 | 位置 | 现状 |
|---|---|---|
| 主控 | `03-skills/SKILL.md` | 九节点，终点仍是 MCP `publish_content`；②对标研究工具映射仍写 MCP |
| 节点 prompt | `03-skills/prompts/`（6 个） | `发布器.md` 仍是 MCP 发布执行员 |
| 工具映射 | `04-工具/工具映射.md` | §1 保留 publish 主路径；生图走 image-gen-mcp（gpt-image-2-official，apimart 异步补丁） |
| 本地脚本 | `render.mjs` / `verify.mjs` / `normalize-cover.mjs` / `normalize-image.mjs` | 全部可用；依赖 puppeteer ^23（项目根 package.json） |
| 视觉 skill | `.claude/skills/cover-anchor-system`、`.claude/skills/xhs-visual-director` | 已装；上游分别 2026-06-22 / 2026-07-03 有更新 |
| run schema | `00-总览/术语与产物约定.md` §2.2 | 六态 status + confirmations + degradations |
| 实际用法 | `06-产出/20260714-rainy-desk-reset/run.yaml` | **已事实偏离主控**：research tool 记 `agent-reach(OpenCLI->xiaohongshu)`，confirmations 为空，notes 写明「按用户要求跳过发布」 |
| 研究后端 | `agent-reach doctor --json` | `xiaohongshu.active_backend = OpenCLI`，`github = gh CLI` |

结论：最新 run 已经在按目标形态跑，本次升级本质是**把事实做法写进制度**，并补齐两道闸门与路由层。

## 4. 目标架构

```text
① 档案加载
② 只读研究          （agent-reach OpenCLI 首选 / redbook 增强 / xiaohongshu-mcp 兜底）
③ Topic Gate        （新增：选题准入审查，go / revise / reject）
④ 选题定角 + ⑤ 分镜规划
⑥ Style Router      （新增：按注册表路由封面/内页 skill，支持手动指定）
⑦ 封面生产 → ★1 视觉母版确认 → ⑧ 内页生产
⑨ Visual Gate       （新增：程序化校验 + 多模态读图复核，pass / revise / fallback）
⑩ 文案定稿 → ⑪ 组包终审（verify.mjs）
★2 发布包确认 → ⑫ Publish Pack（人工发布）→ 手动发布后补录归档
```

## 5. 二次调研结果（2026-07-14 实测，gh CLI + 源码核读）

### 5.1 核心吸收项目复核（均在维护中）

| 项目 | Star | 最近推送 | 本次新增的技术情报 |
|---|---|---|---|
| [op7418/guizang-social-card-skill](https://github.com/op7418/guizang-social-card-skill) | 5022 | 2026-07-01 | ① 自带 `validate-social-deck.mjs`：程序化版式校验 R1–R9（溢出/页脚碰撞/最小字号/4 段密度/标题间距/视觉边界），`node validate-social-deck.mjs <task-dir|index.html> [--style=]`，FAIL 退出码 1；② 依赖 **playwright ^1.60**（与本项目 puppeteer 独立，需在 skill 目录内 `npm i`）；③ 页面规格恰为 1080×1440（与本项目 3:4 完全一致）；④ `references/category-cookbook.md` 明确「能力圈」：OOTD 全身 / 美食摆盘大片 / 梦核氛围 / Y2K 装饰系**明示做不了**——可直接转成路由拒绝规则；⑤ `references/qa-checklist.md` 含溢出修正阶梯（1-40px 只调间距，160px+ 才换版式）与 4 段密度判据 |
| [ziguishian/xhs-visual-director-skill](https://github.com/ziguishian/xhs-visual-director-skill) | 1030 | 2026-07-03 | 上游 07-03 有「Update visual director workflow」提交，本地副本宜做一次 diff 同步（只同步 docs/，不引入其 10 问前端）；其 `visual_consistency_protocol.md`、`anti_patterns.md` 是 Visual Gate 检查项直接素材 |
| [lucasygu/redbook](https://github.com/lucasygu/redbook) | 384 | 2026-07-04 | ① `npm i -g @lucasygu/redbook`，**Node ≥ 22**；② **Windows 坑**：Chrome 127+ App-Bound Encryption，自动读 cookie 需先关闭 Chrome，失败可 `--cookie-string` 手动传；③ 研究向命令：`search --sort popular / read / comments --all / topics / analyze-viral / viral-template --json`；④ 额外价值：`redbook health` 可检测**自己已发笔记**是否被隐形限流——适合并入「手动发布后补录」环节；⑤ 写操作命令明确存在（post/comment/reply/batch-reply/collect），必须列入禁用黑名单 |
| [liangdabiao/XHS_Business_Idea_Validator](https://github.com/liangdabiao/XHS_Business_Idea_Validator) | 208 | 2026-01-06 | **半年未更新**，维持原判：不整包接入，只吸收「抓取→评论分析→可判定报告」的阶段化思路做 Topic Gate |
| [ponyodong2026/ponyo-cover-anchor-system](https://github.com/ponyodong2026/ponyo-cover-anchor-system) | 140 | 2026-06-22 | 维持原判：降格为封面专项 skill；其 `cover-diagnosis-checklist.md` 已在本地副本中，直接作为 Visual Gate 封面分项 |

### 5.2 新调研项目（本轮新增核查）

| 项目 | Star | 判定 | 理由 |
|---|---|---|---|
| [white0dew/XiaohongshuSkills](https://github.com/white0dew/XiaohongshuSkills) | 3151 | **反例，不接入** | CDP 自动发布 + 自动评论；其 README 自带风险提示「存在被平台风控、限流、封号的风险……建议对最终发布内容人工复核」——**赛道最高星项目自己都这么写**，反向坐实了本方案的人工发布决策 |
| [vivy-yi/xiaohongshu-skills](https://github.com/vivy-yi/xiaohongshu-skills) | 295 | 知识库参考，不接执行链 | 139 个运营知识 SKILL.md（平台规则 14 个、数据分析 12 个），Topic Gate 的判据措辞可从中取材；无代码执行面，不构成依赖 |
| [freestylefly/xiaohongshu-skills](https://github.com/freestylefly/xiaohongshu-skills) | 93 | 不接入 | Nano Banana Pro 封面生成，但绑定第三方 canghe.ai API key，引入新付费依赖，与现有 image-gen-mcp 通道重复 |
| [Vivixiao980/xhs-cover-skill](https://github.com/Vivixiao980/xhs-cover-skill) | 90 | 观察，不接入 | 18 风格封面 CLI，与 ponyo + guizang 能力重叠 |
| [DeliciousBuding/xiaohongshu-skill](https://github.com/DeliciousBuding/xiaohongshu-skill) | 36 | 不接入 | Playwright 读写一体（含发布/互动/配额 SOP）；读能力与 OpenCLI 重复，写能力违反约束 2/6 |

### 5.3 调研对方案的三点修正

1. **「style router」没有现成轮子**。GitHub 全量搜索未见可复用的视觉技能路由实现，自研 `style-registry.yaml` + 路由规则表（§8.1）是正确且必要的路径。
2. **Visual Gate 不需要新依赖**。实现 = 两层：确定性层（现有 verify.mjs 3:4 校验 + guizang validator 管 HTML 页）+ 多模态层（Claude 本身用 Read 工具直接读 PNG 逐张审图——harness 原生能力，零成本）。原方案设想的「引入图像 QA 工具」可以取消。
3. **发布后数据回流有了轻量入口**。原 schema 中 `已发档案.jsonl` 的 `metrics` 一直是 null；redbook `health`（限流检测）与 `user-posts` 可在「手动发布后补录」时顺手回填，属可选增强，不进主链。

## 6. MCP 与外部工具边界（写死）

| 工具 | 允许 | 禁止 |
|---|---|---|
| xiaohongshu-mcp | `check_login_status`、`search_feeds`、`get_feed_detail`（仅作研究**兜底**） | `publish_content` 及一切评论/点赞/收藏/互动工具 |
| agent-reach (OpenCLI) | `opencli xiaohongshu search/note`（研究**首选**） | — |
| redbook（可选） | `whoami / search / read / comments / topics / feed / user / user-posts / analyze-viral / viral-template / health / boards / board` | `post / comment / reply / batch-reply / collect / uncollect` |
| image-gen-mcp | `generate_image`（产线乙生图，现有通道不变） | — |

> xiaohongshu-mcp 自本版起仅作为**研究兼容后端**保留，不再作为发布通道。本地两个 exe（登录器/服务器）保留不删，供兜底时启动。

## 7. Agent 执行任务清单（按序执行，每条含验收标准）

> 通用约定：所有改动以本项目根（`小红书/`）为工作目录；改文档时保持原文件的章节编号风格；每完成一个任务在本文件 §10 进度表打勾。

### T1 · 主控 SKILL.md 改造（Phase 1 核心）

**文件**：`03-skills/SKILL.md`

改动规格：

1. frontmatter `description` 去掉「经 xiaohongshu-mcp 发布并归档」，改为「双闸门复核后产出人工发布包（Publish Pack），发布由用户手动完成后补录归档」。
2. §2 前置检查：第 2 条 MCP 登录检查改为「研究后端体检」——先跑 `agent-reach doctor --json` 读 `xiaohongshu.active_backend`；OpenCLI 可用即通过；不可用时才检查 xiaohongshu-mcp 登录态；两者皆不可用 → 研究节点预记降级。新增第 3 条：检查 `03-skills/style-registry.yaml` 存在且所列 skill 目录存在（缺失的 skill 在路由时不可选）。
3. §3 节点表改为 §4 目标架构的 12 节点（新增 `topic_gate`、`style_route`、`visual_gate`、`publish_pack` 四行；`publish` 行删除）。新节点的「执行」列分别指向 `prompts/选题闸门.md`、`03-skills/style-router.md`、`prompts/视觉复核器.md`、`prompts/发布包生成器.md`。
4. ② 对标研究行的工具改为：「首选 agent-reach OpenCLI；增强 redbook（若已装）；兜底 xiaohongshu-mcp」；产物加 `research-evidence.json`。
5. §5 确认点：★1 不动；★2 改名「发布包确认」——呈现 manual-publish.md 预览 + 3 标题候选，用户确认后 run 状态置 `package_ready`，**不再有「现在发还是定时」话术**；新增固定话术：「发布包已就绪：`06-产出/<run>/manual-publish.md`。请手动发布后把笔记链接发我，我来补录归档（可顺带做限流检测）。」
6. §7 降级路由：删除 ⑨ 发布失败条目；新增 `topic_gate=reject → 流程终止并给出改题建议`、`topic_gate=revise → 回②补充研究或换角度，最多 2 轮`、`visual_gate 单页 fallback → 按 style-registry 的 fallback 链降级该页`。
7. §8 红线追加一条：「**不调用 `publish_content`**。发布动作只能由用户手动完成。」

**验收**：全文无 `publish_content` 作为主路径的表述；12 节点表完整；`waiting_confirm/waiting_backfill` 断点续跑语义（§4）未被破坏。

### T2 · 工具映射.md 更新（Phase 1）

**文件**：`04-工具/工具映射.md`

改动规格：

1. §1 MCP 表：删「⑨ 发布」两行；`search_feeds/get_feed_detail` 标注「研究兜底（第三优先）」；表下加边界声明（本文件 §6 的表格照搬）。
2. 新增 §1.5「研究后端优先级」：`OpenCLI（agent-reach）→ redbook（可选增强）→ xiaohongshu-mcp（兜底）`，附各自失败时的降级动作与「三者皆不可用 → research 记 degraded 未对标」。
3. §2 本地脚本表新增一行：guizang validator——`node .claude/skills/guizang-social-card-skill/validate-social-deck.mjs <html目录或index.html>`，退出码 0/1/2，用途「html_png 页版式校验（R1-R9）」（T6 完成后此行才生效，可先写注明依赖 T6）。
4. §3 生图通道不动（image-gen-mcp 现有配置、超时经验、降级链全部保留）。

**验收**：文档内 `publish_content` 仅出现在「已废弃/禁止」语境中。

### T3 · 新建选题闸门 prompt（Phase 3，可与 T4 并行）

**文件**：新建 `03-skills/prompts/选题闸门.md`

规格（prompt 文件按现有 6 个节点 prompt 的 System/User/指令/输出/通过标准 结构写）：

- **输入**：`research.md`、`research-evidence.json`、`05-验收/已发档案.jsonl`、`06-产出/` 下最近 10 个 run 的 `run.yaml`（读 topic 字段做查重）。
- **六项评分**（每项 0-2 分，必须引用 research-evidence 中的具体笔记数据作为证据，不许凭空打分）：
  1. 重复风险（与最近 7-10 篇主题距离）
  2. 竞争密度（同题笔记的互动量分布：头部是否垄断、腰部是否有空隙）
  3. 评论需求度（评论区是否有明确未被满足的问题/情绪）
  4. 图文化适配度（适不适合 6-8 页 3:4 图文）
  5. 风格可视化潜力（有无明确视觉锚点）
  6. 人工发布可行性（账号节奏、发布频次）
- **判定**：总分 ≥9 → `go`；6-8 → `revise`（附具体改角度建议）；≤5 → `reject`（附换题建议）。阈值写在文件头部，允许用户日后调整。
- **输出**：`<run>/topic-gate.md`（人读报告）+ `<run>/topic-score.json`（schema 见 §8.2）；run.yaml `topic_gate` 节点记 status 与 verdict。
- 设计参考已消化进上述规格：XHS_Business_Idea_Validator 的「可判定报告」思路、redbook `analyze-viral` 的互动比例维度、vivy-yi 平台规则/数据分析技能的判据措辞。

**验收**：对任一历史 run 的 research.md 干跑一次能产出合法 topic-score.json（六项、verdict、evidence 引用齐全）。

### T4 · 新建视觉复核器 prompt（Phase 3）

**文件**：新建 `03-skills/prompts/视觉复核器.md`

规格：

- **触发**：节点⑧内页生产完成、`images/` 齐全后。
- **两层检查**：
  - L1 确定性（先跑，快速拦截）：`node 04-工具/verify.mjs <run>` 的图片分项必须已过；本 run 若有 guizang/HTML 产线页 → 对其 html 跑 guizang validator（T6），FAIL 即该页直接 `revise`。
  - L2 多模态（逐张 Read 读图）：用 Read 工具逐张打开 `images/cover.png`、`images/page-NN.png`，按六项 checklist 评审：① 3:4 构图完整无裁切感 ② 手机缩略图下标题/重点字可读 ③ 页间色彩/字重/构图语言一致 ④ 中文零乱码错字（引号/等号/断句重点核）⑤ 每页只有一个传播任务 ⑥ 无廉价 AI 感（蓝紫渐变/PPT 感/信息过载）。封面额外过 `cover-anchor-system/references/cover-diagnosis-checklist.md`；内页一致性判据引用 `xhs-visual-director/docs/visual_consistency_protocol.md` 与 `anti_patterns.md`。
- **判定**：逐页 `pass / revise / fallback`。`revise` = 重生成该页（沿用现有重试上限 2 轮）；`fallback` = 按 style-registry 的 fallback 链降级（AI 页 → guizang 卡片 → 产线甲 HTML 模板），run.yaml 记 degradation。
- **输出**：`<run>/visual-gate.md` + `<run>/visual-review.json`（schema 见 §8.3）；run.yaml `visual_gate` 节点记 status。
- **与 ★1 的关系**：★1 仍是「母版方向」的人工确认；Visual Gate 是全套成图后的系统性复核，两者不互相替代。

**验收**：对 `06-产出/20260714-rainy-desk-reset`（已有全套成图）干跑一次，产出合法 visual-review.json 且逐页有 verdict + 理由。

### T5 · 新建风格注册表与路由器（Phase 2）

**文件**：新建 `03-skills/style-registry.yaml`（内容见 §8.1，直接照抄）+ 新建 `03-skills/style-router.md`

`style-router.md` 规格：

- **三种模式**：
  1. 手动指定：用户在触发或 ★1 反馈中说「封面用 X / 内页用 Y」→ 直接生效并记 run.yaml；
  2. 半自动：用户只给风格家族（暖纸手绘 / 瑞士理性 / 杂志叙事 / 高对比信息卡）→ 按 registry 的 `families` 字段映射；
  3. 自动路由（默认）：按决策表——封面：情绪/场景/人物题 → `cover-anchor-system`；纯文字/数字冲击 → `cover-anchor-system`，中文极密时 `guizang`。内页：强视觉叙事页 → `xhs-visual-director`；清单/对比/数据/密集中文页 → `guizang`；全部失败 → `html-templates`（产线甲）。
- **拒绝规则**（来自 guizang category-cookbook 能力圈，路由前先判）：日常 OOTD 全身、美食摆盘大片、梦核/氛围装饰、Y2K 装饰系 → 任何 skill 都不接，回到分镜改用实拍回填协议或改题。
- 路由结果写 `<run>/prompts/style-decision.md` + run.yaml `visual_router` 块。

**验收**：yaml 可被解析（`node -e "..."` 或人工核）；对 3 个虚拟题材（教程干货 / 情绪场景 / 数据清单）口头干跑路由，结果符合决策表。

### T6 · 接入 guizang-social-card-skill（Phase 2）

命令序列：

```bash
git clone https://github.com/op7418/guizang-social-card-skill ".claude/skills/guizang-social-card-skill"
cd .claude/skills/guizang-social-card-skill && npm install   # playwright ^1.60，与项目根 puppeteer 互不影响
npx playwright install chromium                               # 若本机无 playwright chromium（约 300MB）
node validate-social-deck.mjs --help 2>&1 | head -3           # 冒烟：能打印 usage 即可
```

作用域限制（写入主控 §3 注记，与 ponyo/visual-director 同格式）：

- 只使用**社交卡片图文**能力；**不使用** Live Photo、微信封面对、发布建议等前端；
- 任务目录必须显式指定为 `06-产出/<run>/html/guizang/`（该 skill 默认写 `local-tests/`，须覆盖）；
- 出图桥接：guizang 渲染 PNG → 拷入 `<run>/assets/page-NN-src.png` → `node 04-工具/normalize-image.mjs <run> assets/page-NN-src.png images/page-NN.png`（与产线乙同一归一化路径，**不直接进 images/**）；
- 其页面本身即 1080×1440，normalize 只做兜底归一，预期无损。

**验收**：clone + npm i 成功；validator 冒烟通过；用其 swiss 模板出 1 张测试卡并 normalize 到 3:4 通过 verify 图片分项。

### T7 · 发布器改造为发布包生成器（Phase 1 核心）

**文件**：`03-skills/prompts/发布器.md` → 重写并更名 `03-skills/prompts/发布包生成器.md`（旧文件删除）

规格：

- **前置**：★2「发布包确认」已 approved、verify.mjs 全过、visual_gate=pass。
- **产出** `<run>/manual-publish.md`，结构固定：① 推荐标题（用户选定项置顶）② 备用标题 ×2 ③ 正文（可直接复制）④ 标签行 ⑤ 评论区首条引导语 + 「发布后 1 小时内手动贴」提醒 ⑥ 推荐发布时间（默认 20-22 点档）⑦ 发布前人工核对项（引用 `05-验收/发布前检查清单.md` 软判据摘要）⑧ 图片顺序清单（cover → page-01 → …，给绝对路径）。
- **补录协议**（替代原 receipt 逻辑）：用户手动发布后发回笔记链接 → 写 `<run>/receipt.md`（URL、发布时间、补录时间）→ 追加 `05-验收/已发档案.jsonl`（字段不变，metrics 仍可 null）→ run.yaml `publish.status: manually_published`。若装了 redbook，可选跑 `redbook health` 与 `redbook read <url>` 回填 metrics 初值。
- 原「防重复发布」段落整段删除（人工发布天然无此问题）。

**验收**：对任一历史 run 干跑产出合法 manual-publish.md；`05-验收/已发档案.jsonl` 追加行格式与旧行一致。

### T8 · run.yaml schema v2 与文档同步（Phase 1 收尾）

**文件**：`00-总览/术语与产物约定.md`（§2.1、§2.2）、`00-总览/使用说明.md`

改动规格：

1. §2.2 run.yaml 增量字段（新 run 必写，见 §8.4）；六态 status 枚举**不变**。
2. §2.1 运行目录树补：`research-evidence.json`、`topic-gate.md`、`topic-score.json`、`visual-gate.md`、`visual-review.json`、`manual-publish.md`；`receipt.md` 注释改为「手动发布补录后存在」。
3. **兼容规则写死**：run.yaml 无 `workflow_mode` 字段的旧 run，「继续 <run>」时按旧九节点语义走完，不强制迁移；新 run 一律 `workflow_mode: slim_manual_publish`。
4. 使用说明.md 的触发示例与流程简介同步为新架构。

**验收**：新旧两种 run.yaml 各构造一份样例，断点续跑规则均可无歧义解释。

### T9 ·安装 redbook 研究增强

```bash
node -v                                  # 须 >= 22，否则跳过本任务
npm install -g @lucasygu/redbook
redbook whoami                           # Windows：Chrome 127+ 需先完全关闭 Chrome 再跑；失败用 --cookie-string
```

- 成功 → 在工具映射 §1.5 把 redbook 状态改为「已装」；研究节点可用 `analyze-viral / viral-template --json` 产出 Topic Gate 证据。
- 失败/跳过 → 不阻塞任何任务，OpenCLI 已覆盖基本研究面。
- **黑名单写入工具映射**：`post / comment / reply / batch-reply / collect / uncollect` 禁用。

### T10 · 端到端干跑验收（总验收）

用一个测试主题完整走 `① → ⑫`（生图可用 quality=medium 省额度，或复用历史素材）：

- [ ] research-evidence.json / topic-score.json / visual-review.json / manual-publish.md 四个新产物齐全且 schema 合法
- [ ] run.yaml 含 §8.4 全部新字段，六态语义正常
- [ ] `node 04-工具/verify.mjs <run>` 退出码 0
- [ ] 全程零调用 `publish_content` 与互动类工具
- [ ] 中断一次后「继续 <run>」能正确续跑

### T11 · 代码与文档规范落地（Phase 4 · 仓库化）

**文件**：新建 `.editorconfig`、`.gitattributes`、根 `README.md`、`LICENSE`；修改 `package.json`。规范全文见 §12。

改动规格：

1. 按 §12.4 创建 `.editorconfig` 与 `.gitattributes`（内容照抄）。
2. `package.json` 增加 `"engines": {"node": ">=20"}` 与 scripts：
   ```json
   "scripts": {
     "test": "node 04-工具/normalize-image.test.mjs && node 04-工具/normalize-cover.test.mjs",
     "verify": "node 04-工具/verify.mjs",
     "render": "node 04-工具/render.mjs"
   }
   ```
   （verify/render 需 run 目录参数，调用形如 `npm run verify -- 06-产出/<run>`。）
3. 新建根 `README.md`，必含：项目定位（只读研究 / 可插拔配图 / 双闸门复核 / 人工发布的轻编辑部工作流）、§4 架构图、目录结构说明、安装步骤（Node ≥ 20、`npm i`、三个视觉 skill 的获取方式——其中 ponyo 与 guizang 按 §13.2 只给安装命令不随仓库分发）、触发与断点续跑用法、第三方致谢与许可表（§13.2）、免责声明（本项目不含自动发布 / 自动互动能力）。`03-skills/薄壳触发器副本.md` 的安装说明并入 README 安装节（原文件保留，全局触发器同步留档用途仍在）。
4. 新建 `LICENSE`：建议 MIT（署名项目贡献者）；README 注明 `.claude/skills/` 下第三方 skill 各按其自身许可，不适用本仓库 LICENSE。
5. 存量 4 个 `.mjs` 脚本经实测已符合 §12.1（本规范即从其风格提炼成文），**不做任何重构**（兼容守则优先）；自本任务起新增脚本必须遵循 §12。

**验收**：`npm test` 一条命令跑通两个自测；README 覆盖「克隆 → 安装 → 首跑」完整路径；`git ls-files` 出现 5 个新文件（含 example 档案，见 T12）。

### T12 · 文件精简与仓库瘦身（Phase 4 · 公开前必做）

**执行依据**：§13 清单逐项执行。远端仓库当前为 **private**（2026-07-14 实测），本任务完成前不得 flip public。

动作序列：

1. 删除 §13.1 冗余文件（均未被追踪，直接删）。
2. 许可证合规（§13.2）：ponyo 移出追踪、guizang 预先 gitignore。
3. 隐私处理（§13.3）：账号档案模板化；已发档案与历史重写两项按用户决策执行。
4. 06-产出 白名单化（§13.4）：改 .gitignore + `git rm -r --cached` 存量。
5. 归档移动（§13.5）：历史计划文档入 `00-总览/history/`；提交根目录定调结论文档的移动（当前处于「根目录已删、00-总览 未追踪」的未提交状态）。
6. 入库新增（§13.6）并提交；公开前终检：`git -c core.quotepath=false ls-files` 全量过目，确认无敏感 / 无授权文件。

**验收**：`git status` 干净；06-产出 追踪文件从 92 个降到白名单范围内；`git ls-files` 中不存在 `cover-anchor-system`、`账号档案.md`（真实版）；README 许可表与实际 vendored 内容一致。

## 8. 关键 Schema（直接照抄使用）

### 8.1 `03-skills/style-registry.yaml`

```yaml
version: 1
families:            # 半自动模式的风格家族映射
  暖纸手绘: cover-anchor-system
  瑞士理性: guizang-social-card-skill
  杂志叙事: guizang-social-card-skill
  高对比信息卡: xhs-visual-director

skills:
  - id: cover-anchor-system
    path: .claude/skills/cover-anchor-system
    role: cover
    strengths: [high-ctr-cover, visual-anchor, chinese-cover-prompt]
    best_for: [冲突型, 数字型, 情绪型, 场景实拍型]
    output_mode: image_prompt        # 出提示词 → image-gen-mcp → normalize-cover
    review_checklist: references/cover-diagnosis-checklist.md
    fallback: guizang-social-card-skill

  - id: xhs-visual-director
    path: .claude/skills/xhs-visual-director
    role: inner
    strengths: [style-system, page-sequencing, visual-consistency]
    best_for: [教程, 方法论, 观点型图文, 科技感, 杂志感]
    output_mode: image_prompt        # 出提示词 → image-gen-mcp → normalize-image
    review_checklist: docs/visual_consistency_protocol.md
    fallback: guizang-social-card-skill

  - id: guizang-social-card-skill
    path: .claude/skills/guizang-social-card-skill
    role: cover_or_inner_fallback
    strengths: [deterministic-layout, swiss-style, editorial-style, html-to-png]
    best_for: [清单, 对比, 数据页, 结构化信息页, 密集中文]
    output_mode: html_png            # html → 渲染 PNG → normalize-image
    validator: validate-social-deck.mjs
    review_checklist: references/qa-checklist.md
    fallback: html-templates
    excluded_topics: [日常OOTD全身, 美食摆盘大片, 梦核氛围装饰, Y2K装饰系]   # 来自其 category-cookbook 能力圈

  - id: html-templates               # 产线甲，最终兜底
    path: 02-规范/templates
    role: terminal_fallback
    strengths: [zero-dependency, deterministic]
    output_mode: html_png            # render.mjs
    fallback: null
```

### 8.2 `<run>/topic-score.json`

```json
{
  "run": "20260715-xxx",
  "verdict": "go",
  "total": 10,
  "threshold": {"go": 9, "revise": 6},
  "scores": {
    "dedup_risk":        {"score": 2, "evidence": "近10篇无同题；最近相邻为 20260712-rent-dehumidify（弱相关）"},
    "competition":       {"score": 2, "evidence": "搜索前20条中腰部笔记(赞500-2000)占7条，存在空隙"},
    "comment_demand":    {"score": 2, "evidence": "note_xxx 评论区高赞问题：『到底先收哪里』×3"},
    "carousel_fit":      {"score": 2, "evidence": "可拆4步骤+1清单，6页结构自然"},
    "visual_anchor":     {"score": 1, "evidence": "有场景锚点但无强数字锚点"},
    "publish_feasible":  {"score": 1, "evidence": "本周已发2篇，节奏偏满"}
  },
  "suggestion": "verdict 非 go 时必填：改角度/换题建议"
}
```

### 8.3 `<run>/visual-review.json`

```json
{
  "run": "20260715-xxx",
  "overall": "pass",
  "l1_verify": "pass",
  "pages": [
    {"file": "images/cover.png",   "producer": "ponyo-ai-cover", "verdict": "pass",
     "checks": {"ratio": true, "thumb_readable": true, "consistency": true,
                "chinese_ok": true, "single_task": true, "no_cheap_ai": true},
     "notes": ""},
    {"file": "images/page-03.png", "producer": "vd-ai-page", "verdict": "fallback",
     "checks": {"chinese_ok": false},
     "notes": "第3行『收纳』错字为『收讷』→ 降级 guizang 卡片重做"}
  ]
}
```

### 8.4 run.yaml v2 增量字段

```yaml
workflow_mode: slim_manual_publish      # 缺省此字段 = 旧版 run，按旧语义续跑
research_backend: opencli | redbook | xiaohongshu-mcp | none
topic_gate:
  status: go | revise | reject
  total: 10
  rounds: 1
visual_router:
  mode: auto | semi | manual
  cover_skill: cover-anchor-system
  inner_skill: xhs-visual-director
  fallback_skill: guizang-social-card-skill
visual_gate:
  status: pass | revise | fallback
  fallback_pages: []                    # 例 ["page-03"]
publish:
  mode: manual_only
  status: package_ready | manually_published | archived_only
  note_url: null                        # 补录时填
```

## 9. 落地顺序与兼容守则

| Phase | 任务 | 效果 |
|---|---|---|
| 1 先削风险 | T1 → T2 → T7 → T8 | 自动发布退出主流程，发布包成为新终点 |
| 2 再做路由 | T5 → T6（T9 可选并行） | 视觉层从固定双产线变为三 skill + 兜底的显式路由 |
| 3 最后补闸门 | T3 → T4 → T10 | 研究后、成图后各一道有判定的准入闸 |
| 4 仓库化 | T11 → T12 | 规范成文、冗余出清、许可合规，仓库可安全公开 |

兼容守则（执行 Agent 必须遵守）：

- **verify.mjs / render.mjs / normalize-*.mjs 一行不改**（本次升级零脚本改动；guizang validator 是外来新增，不与其耦合）。
- 旧 run 目录与 `已发档案.jsonl` 历史行不迁移、不重写。
- `.claude/skills/` 现有两个 skill 不删不改，只在 T5 注册、（可选）与上游 diff 同步 docs。
- 每个 Phase 结束跑一次对应验收再进下一 Phase。

## 10. 执行进度表（Agent 执行时更新）

- [ ] T1 主控改造　- [ ] T2 工具映射　- [ ] T7 发布包生成器　- [ ] T8 schema 同步 —— Phase 1
- [ ] T5 注册表与路由　- [ ] T6 guizang 接入　- [ ] T9 redbook（可选） —— Phase 2
- [ ] T3 选题闸门　- [ ] T4 视觉复核器　- [ ] T10 端到端干跑 —— Phase 3
- [ ] T11 规范落地　- [ ] T12 清理瘦身与公开前终检 —— Phase 4

## 11. 不做清单

- 自动发布 / 自动评论 / 自动点赞收藏 / 自动互动（含 white0dew、DeliciousBuding 的发布与互动面）
- 高强度浏览器自动化发布框架、持续采集型浏览器插件作为默认依赖
- 绑定第三方付费 API 的新生图通道（现有 image-gen-mcp 通道够用）
- 把工作流拆成多条独立流水线（方案 C，当前规模不需要）

## 12. 统一代码与文档规范（T11 执行依据）

> 本规范不是外来标准，而是从现有 4 个 `.mjs` 脚本与 6 个节点 prompt 的既成风格**提炼成文**——存量代码天然合规，零重构成本；约束的是今后的新增与修改。

### 12.1 脚本规范（`04-工具/*.mjs`）

- **运行时**：Node ≥ 20；纯 ESM（`.mjs`，`package.json` 已 `"type": "module"`）；内置模块一律 `node:` 前缀（`node:fs/promises`、`node:path`）。
- **依赖纪律**：默认零依赖；根 `package.json` 当前唯一允许 puppeteer ^23；新增任何依赖须先在 `04-工具/工具映射.md` 写明理由再装。第三方 skill 的依赖（如 guizang 的 playwright）只装在其 skill 目录内，**不进根 package.json**。
- **文件头契约**（三要素缺一不可，新脚本照此写）：

  ```js
  // 04-工具/<name>.mjs — <一句话用途>（规格 §N 或「新增：<出处>」）
  // 用法: node 04-工具/<name>.mjs <参数表>
  // 退出码: 0=成功 / 1=内容校验不过 / 2=用法或环境错误
  ```

- **退出码契约**（全项目统一，guizang validator 恰好同构）：`0` 成功 / `1` 内容不合格（打印 `FAIL` 行，一次性列全所有问题）/ `2` 用法或环境错误（立即报错退出）。
- **错误处理**：校验类问题收集进 `errors[]` 数组最后统一输出（参照 verify.mjs），不要遇错即停；环境类问题（目录不可读、浏览器起不来）立即 `console.error` + `exit 2`。
- **命名**：脚本文件 kebab-case 纯英文；常量 `UPPER_SNAKE`（如 `W / H / DSF`）；导出函数 camelCase；需要 CLI + 模块双形态时参照 `normalize-image.mjs`（`export` 函数 + `import.meta.url` 判定 CLI 入口）。
- **注释**：中文；只写「为什么 / 约束 / 实测坑」（如 file:// 子资源拦截的说明），不复述代码在做什么；重要经验注明实测日期。
- **测试**：与被测脚本同目录，命名 `<name>.test.mjs`；**零测试框架**，`node` 直接运行；用 `spawnSync` 走真实 CLI 验证退出码契约；fixture 自建自清，统一放 `06-产出/00000000-*`；失败收集进 `fails[]`，退出码非零。

### 12.2 文档规范（`.md` / prompt / schema）

- **一文一职责**：`00-总览/工作流规格说明.md` 是唯一权威；其他文件抄录规格时必须标注来源章节（现行做法，成文固化）。
- **节点 prompt 固定五段**：`System / User（变量）/ 指令 / 输出 / 通过标准`——T3/T4/T7 新建的 prompt 一律沿用此结构。
- **排版**：中英文之间加空格；路径、代码、工具名用反引号；内部引用一律相对路径 markdown 链接。
- **命名**：产物与代码文件**纯英文 kebab-case**（verify.mjs 有硬校验）；说明文档可用中文文件名（项目面向中文用户，GitHub 完全兼容）；机器可读文件（yaml/json）的键名一律英文 snake_case（run.yaml 现状即如此）。
- **产物登记**：run 目录内新增任何产物类型，先在 `00-总览/术语与产物约定.md` §2.1 登记再使用。

### 12.3 提交规范

- Conventional Commits：`feat / fix / docs / test / chore`（现有 git log 已自然成型，成文沿用）；描述可中文；一次提交只做一件事；**run 产物提交与代码/文档提交分开**。

### 12.4 配置文件内容（T11 直接照抄）

`.editorconfig`：

```ini
root = true

[*]
charset = utf-8
end_of_line = lf
insert_final_newline = true
indent_style = space
indent_size = 2
trim_trailing_whitespace = true

[*.md]
trim_trailing_whitespace = false
```

`.gitattributes`：

```gitattributes
* text=auto eol=lf
*.png binary
*.webp binary
*.exe binary
06-产出/** linguist-generated
```

## 13. 文件精简与仓库化清理清单（T12 执行依据）

> 基线（2026-07-14 实测）：`.git` 已存在，remote 指向私有仓库（默认分支 master，本地功能分支领先 14 提交）；`git ls-files` 共 168 个文件，其中 92 个在 `06-产出/`；磁盘上 `06-产出/` 已达 399MB；cookies.json / *.exe / node_modules 已被 .gitignore 覆盖（未追踪，安全）。

### 13.1 冗余文件——直接删除（均未被 git 追踪，无历史影响）

| 路径 | 体量 | 原因 |
|---|---|---|
| `06-产出/20260703-after-gaokao - 副本/` | 23MB | 完整重复目录（正本仍在），且中文+空格目录名违反 §12.2 命名规范 |
| `06-产出/00000000-selftest/degrade-demo/` | 小 | 降级演示遗留物，非 fixture 本体 |
| `06-产出/20260702-rent-traps/images/ponyo-gen-cover.png` | 小 | 游离 AI 源图放错位置（约定：源图进 `assets/`，`images/` 只放成品）；有留存价值则移 `assets/` |

### 13.2 第三方 skill 许可证合规（公开前必做，2026-07-14 逐项核验）

| skill | 上游许可 | 当前 git 状态 | 处置 |
|---|---|---|---|
| `xhs-visual-director` | **MIT** ✓ | 已 vendored 已追踪（32 文件） | **可保留入库**：保留其 LICENSE 文件，README 致谢原作者 ziguishian |
| `cover-anchor-system`（ponyo） | **无 LICENSE**（gh api 确认 license=null） | 已 vendored 已追踪（8 文件） | **移出追踪**：`git rm -r --cached .claude/skills/cover-anchor-system` + 加入 .gitignore；README 写安装指引（`git clone https://github.com/ponyodong2026/ponyo-cover-anchor-system`）。本地文件保留，日常使用不受影响——私用无碍，**公开再分发无授权依据** |
| `guizang-social-card-skill` | **AGPL-3.0 + 商业双许可**（LICENSE + COMMERCIAL_LICENSING.md） | 未安装（T6 将装） | **预先 gitignore** `.claude/skills/guizang-social-card-skill/`：避免 AGPL copyleft 传染到本仓库的 MIT 许可与潜在商业授权争议；安装命令写 README（T6 已含） |

### 13.3 隐私与个人数据（含两处用户决策项）

| 文件 | 状态 | 处置 |
|---|---|---|
| `cookies.json` / 两个 `*.exe` / `node_modules/` | 已 gitignore ✓ | 本地保留，无动作 |
| `01-账号/账号档案.md` | **已追踪** | `git rm --cached` + gitignore；新建脱敏模板 `01-账号/账号档案.example.md` 入库（保留首跑引导价值） |
| `05-验收/已发档案.jsonl` | 已追踪 | 内容为自己已公开笔记的链接与数据，敏感度低——默认**保留**；若介意则同上模板化（**用户决策 ①**：应该进行模板化） |
| `06-产出/*/research.md` | 部分已追踪 | 含竞品笔记摘录与链接，随 §13.4 白名单化自然退出追踪 |
| git 历史 | 旧提交含账号档案与历史 run 图片 | flip public 且介意历史可见性 → `git filter-repo` 重写后强推（**用户决策 ②**：应该进行重写）；不介意则忽略——当前 remote 是 private，无即时风险 |

### 13.4 `06-产出/` 白名单化（仓库瘦身核心）

现状不可持续：磁盘 399MB、追踪 92 文件且逐 run 增长。改为「产出目录本质是本地工作区，仓库只留 fixture + 一个展示样例」：

`.gitignore` 追加（**必须写 `06-产出/*` 而非 `06-产出/`**，目录级 ignore 无法被 `!` 反选）：

```gitignore
06-产出/*
!06-产出/00000000-selftest/
!06-产出/00000000-verify-fixture/
!06-产出/20260704-declutter-big-first/
06-产出/20260704-declutter-big-first/assets/
```

- 展示样例选 `20260704-declutter-big-first`：首篇产线乙全 AI 成品（工作流里程碑），展示价值最高；其 `assets/`（AI 源图中间产物）单独排除以控体积。
- 存量其余已追踪 run：`git rm -r --cached` 逐个移出（文件本地保留）。

### 13.5 归档移动（不删除，保留可追溯性）

| 文件 | 去向 |
|---|---|
| `00-总览/实现计划.md`、`00-总览/2026-07-03-ponyo封面生图融合-设计.md`、`00-总览/2026-07-03-ponyo封面生图融合-实现计划.md`、`00-总览/2026-07-04-内页visual-director融合-实现计划.md` | 移入 `00-总览/history/`（均为已完成的历史计划，主目录只留 5 份现行文档：工作流总设计 / 工作流规格说明 / 术语与产物约定 / 使用说明 / 定调结论） |
| 根目录 `小红书成稿工作流定调结论.md` | 已移至 `00-总览/`（当前未提交：根目录记 D、新位置未追踪），T12 一并 `git add` 提交 |
| 本升级方案 | T10 总验收通过后同样移入 `00-总览/history/` |

### 13.6 入库新增（配合 T11）

`README.md`、`LICENSE`、`.editorconfig`、`.gitattributes`、`01-账号/账号档案.example.md`、`.mcp.json`（仅 localhost URL，无敏感信息，入库便于他人复现 MCP 兜底配置）。

## 14. 调研参考链接

核心吸收：[guizang-social-card-skill](https://github.com/op7418/guizang-social-card-skill) · [xhs-visual-director-skill](https://github.com/ziguishian/xhs-visual-director-skill) · [redbook](https://github.com/lucasygu/redbook) · [ponyo-cover-anchor-system](https://github.com/ponyodong2026/ponyo-cover-anchor-system) · [XHS_Business_Idea_Validator](https://github.com/liangdabiao/XHS_Business_Idea_Validator)

本轮新核查：[white0dew/XiaohongshuSkills](https://github.com/white0dew/XiaohongshuSkills)（反例）· [vivy-yi/xiaohongshu-skills](https://github.com/vivy-yi/xiaohongshu-skills)（知识库）· [DeliciousBuding/xiaohongshu-skill](https://github.com/DeliciousBuding/xiaohongshu-skill) · [freestylefly/xiaohongshu-skills](https://github.com/freestylefly/xiaohongshu-skills) · [Vivixiao980/xhs-cover-skill](https://github.com/Vivixiao980/xhs-cover-skill)

历史参考（v1 已评估，维持原判不接入）：xiaohongshu-content-collector · skill-workflow · xiaohongshu-automation-skills · xhs-workflow-skill（反例）· Red-Scraper（反例）
