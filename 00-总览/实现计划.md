# 小红书成稿工作流 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 按 [`工作流规格说明.md`](工作流规格说明.md)（下称"规格"）搭建小红书图文创作-发布工作流：约 23 个新文件（规范/模板/prompts/脚本/薄壳 skill），通过规格 §11 全部验收项。

**架构：** 分层轻编辑部——`02-规范/` 存可执行判据，`03-skills/` 存主控编排与节点提示词，`04-工具/` 存两个确定性 Node 脚本（puppeteer 渲染 + 零依赖硬校验），产物按规格 §4 schema 落盘 `06-产出/`。发布通道复用已部署的 xiaohongshu-mcp。

**技术栈：** Markdown（规范与提示词）、单文件 HTML+内嵌 CSS（版式模板）、Node.js ESM + puppeteer（渲染）、Node 零依赖脚本（校验）、xiaohongshu-mcp（HTTP MCP @ localhost:18060）。

**工作目录：** 全部路径相对于 `E:\Learning Files\Codex Project\General Codex Projects\小红书\`（下称"项目根"）。规格中已有的文件（`.mcp.json`、exe、cookies.json、定调结论、规格本体）一律不动。

**执行者须知：**
- 规格是唯一事实源。本计划给出全部骨架与关键内容；凡标注「内容密度见规格 §6.x」的 Markdown 文件，写完后必须逐条对照该节的清单自查。
- 所有含图片路径的文件名/目录名用纯英文小写短横线。
- Windows 环境，命令用 PowerShell 语法给出；涉及 npm 的步骤若遇网络问题先解决再继续，不要跳过验证。

---

## 文件结构总览（创建清单）

| # | 文件 | 职责 | 任务 |
|---|---|---|---|
| 1 | `.gitignore` | 排除敏感与生成物 | 0 |
| 2 | `package.json` | 唯一依赖 puppeteer | 1 |
| 3 | `04-工具/render.mjs` | HTML→PNG 渲染 + 溢出检测 | 2 |
| 4 | `02-规范/templates/card-statement.html` | 观点大字卡（兼降级封面） | 3 |
| 5-8 | `02-规范/templates/card-{checklist,compare,quote,cta}.html` | 其余四版式 | 4 |
| 9 | `04-工具/verify.mjs` | §9 硬校验程序化实现 | 5 |
| 10 | `02-规范/平台硬约束.md` | 硬约束与校验方式 | 6 |
| 11 | `02-规范/封面锚点判据.md` | 6 模板 + 5 项诊断 | 6 |
| 12 | `02-规范/视觉风格库.md` | 8 风格 + 黑名单 | 6 |
| 13 | `02-规范/内页版式库.md` | 5 模板索引与变量表 | 6 |
| 14 | `02-规范/文案规范.md` | 标题公式/结构/标签/评论引导 | 6 |
| 15 | `02-规范/风控清单.md` | 禁词 JSON 词表 + 敏感话题 | 6 |
| 16 | `01-账号/账号档案.md` | 初始档案（探索模式） | 7 |
| 17 | `00-总览/术语与产物约定.md` | 规格 §1+§4 展开 | 7 |
| 18 | `00-总览/工作流总设计.md` | 入口索引 + 一图流 | 7 |
| 19-24 | `03-skills/prompts/{选题定角器,分镜规划器,封面导演,内页生成器,文案生成器,发布器}.md` | 六节点提示词 | 8 |
| 25 | `03-skills/SKILL.md` | 主控编排 | 9 |
| 26 | `05-验收/发布前检查清单.md` | 终审判据 | 10 |
| 27 | `05-验收/已发档案.jsonl` | 空归档文件 | 10 |
| 28 | `04-工具/工具映射.md` | MCP/脚本/降级映射 | 10 |
| 29 | `C:\Users\Endercloud\.claude\skills\xhs-workflow\SKILL.md` | 薄壳触发器 | 11 |

（临时测试夹具建在 `06-产出/00000000-selftest/`，验收后保留作回归样本。）

---

### 任务 0：Git 仓库与 .gitignore

**文件：** 创建 `.gitignore`（项目根）

- [ ] **步骤 1：初始化仓库并写 .gitignore**

```powershell
Set-Location "E:\Learning Files\Codex Project\General Codex Projects\小红书"
git init
```

创建 `.gitignore`：

```gitignore
cookies.json
*.exe
*.zip
node_modules/
```

注意：`cookies.json` 是登录态凭证，**绝不能进版本库**；`06-产出/` 有意纳入版本库（产物即资产）。

- [ ] **步骤 2：首次 commit**

```powershell
git add .gitignore 小红书成稿工作流定调结论.md 00-总览/
git commit -m "chore: init repo with spec and design docs"
```

预期：commit 成功，`git status` 显示 cookies.json 未被跟踪。

---

### 任务 1：Node 依赖（puppeteer）

**文件：** 创建 `package.json`（项目根）

- [ ] **步骤 1：写 package.json**

```json
{
  "name": "xhs-workflow",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "小红书成稿工作流的本地工具依赖",
  "dependencies": {
    "puppeteer": "^23.0.0"
  }
}
```

- [ ] **步骤 2：安装并验证浏览器可启动**

```powershell
npm install
node -e "import('puppeteer').then(async m => { const b = await m.default.launch(); console.log('puppeteer ok:', await b.version()); await b.close(); })"
```

预期：输出 `puppeteer ok: Chrome/1xx...`。若 Chromium 下载失败（规格 §12 已知风险），设置镜像后重装：`$env:PUPPETEER_DOWNLOAD_BASE_URL='https://cdn.npmmirror.com/binaries/chrome-for-testing'; npm install`。

- [ ] **步骤 3：Commit**

```powershell
git add package.json package-lock.json
git commit -m "feat: add puppeteer dependency for HTML-to-PNG rendering"
```

---

### 任务 2：渲染脚本 render.mjs（TDD）

**文件：** 创建 `04-工具/render.mjs`；测试夹具 `06-产出/00000000-selftest/html/`

- [ ] **步骤 1：先建测试夹具（一个好例、一个溢出坏例）**

创建 `06-产出/00000000-selftest/html/good.html`：

```html
<!DOCTYPE html>
<html><head><meta charset="utf-8"><style>
html,body{margin:0;width:1080px;height:1440px;overflow:hidden;background:#FFF8F0;}
h1{font:bold 96px "Microsoft YaHei";color:#1A1A1A;padding:120px 80px 0;}
</style></head><body><h1>渲染自检页</h1></body></html>
```

创建 `06-产出/00000000-selftest/html/overflow.html`（故意超高）：

```html
<!DOCTYPE html>
<html><head><meta charset="utf-8"><style>
html,body{margin:0;width:1080px;}
div{height:2000px;background:#EEE;}
</style></head><body><div>溢出测试</div></body></html>
```

- [ ] **步骤 2：运行尚不存在的脚本，确认失败**

```powershell
node 04-工具/render.mjs 06-产出/00000000-selftest
```

预期：`Error: Cannot find module ...render.mjs`。

- [ ] **步骤 3：实现 render.mjs**

```javascript
// 04-工具/render.mjs — HTML→PNG 渲染（规格 §8.2）
// 用法: node 04-工具/render.mjs <运行目录>  （运行目录下须有 html/，输出到 images/）
import puppeteer from 'puppeteer';
import { readdir, mkdir } from 'node:fs/promises';
import path from 'node:path';
import { pathToFileURL } from 'node:url';

const runDir = process.argv[2];
if (!runDir) { console.error('用法: node render.mjs <运行目录>'); process.exit(2); }
const htmlDir = path.resolve(runDir, 'html');
const imgDir = path.resolve(runDir, 'images');
await mkdir(imgDir, { recursive: true });

const files = (await readdir(htmlDir)).filter(f => f.endsWith('.html')).sort();
if (!files.length) { console.error('html/ 目录为空'); process.exit(2); }

const browser = await puppeteer.launch();
const page = await browser.newPage();
await page.setViewport({ width: 1080, height: 1440, deviceScaleFactor: 2 });

let failed = 0;
for (const f of files) {
  await page.goto(pathToFileURL(path.join(htmlDir, f)).href, { waitUntil: 'networkidle0' });
  const over = await page.evaluate(() => {
    const e = document.documentElement;
    return { h: e.scrollHeight, w: e.scrollWidth };
  });
  if (over.h > 1440 || over.w > 1080) {
    console.error(`FAIL 溢出: ${f} (scroll ${over.w}x${over.h} > 1080x1440)`);
    failed++;
    continue;
  }
  const out = path.join(imgDir, f.replace(/\.html$/, '.png'));
  await page.screenshot({ path: out });
  console.log(`ok: ${path.basename(out)} (2160x2880)`);
}
await browser.close();
process.exit(failed ? 1 : 0);
```

- [ ] **步骤 4：运行验证——坏例被拒、好例出图**

```powershell
node 04-工具/render.mjs 06-产出/00000000-selftest
```

预期：输出 `ok: good.png (2160x2880)` 与 `FAIL 溢出: overflow.html ...`，退出码 1；`images/good.png` 存在。然后删除 `overflow.html` 重跑，预期退出码 0。（保留 overflow.html 为回归样本：改名 `overflow.html.bad` 使其不参与渲染。）

- [ ] **步骤 5：Commit**

```powershell
git add 04-工具/render.mjs 06-产出/00000000-selftest/
git commit -m "feat: puppeteer render script with overflow detection"
```

---

### 任务 3：版式模板一 card-statement.html（兼降级封面）

**文件：** 创建 `02-规范/templates/card-statement.html`

设计要求（规格 §6.4 + §3 节点⑤失败处理）：单文件、内嵌 CSS、`{{变量}}` 占位、1080×1440、变量须同时满足内页观点卡与大字报封面两种用途。

**公共 CSS 约定（本任务定义，任务 4 的四个模板原样复用 `<style>` 头部）：**

```css
/* === 公共基座：每个模板 <style> 开头原样嵌入 === */
html,body{margin:0;padding:0;width:1080px;height:1440px;overflow:hidden;
  font-family:"Microsoft YaHei","PingFang SC",sans-serif;
  background:{{bg_color}};color:#1A1A1A;box-sizing:border-box;}
*,*::before,*::after{box-sizing:inherit;}
.page{width:1080px;height:1440px;padding:96px 80px;display:flex;flex-direction:column;}
.kicker{font-size:36px;letter-spacing:8px;color:{{accent_color}};font-weight:bold;}
.pageno{position:absolute;bottom:48px;right:80px;font-size:28px;color:#999;}
```

- [ ] **步骤 1：写模板**

```html
<!DOCTYPE html>
<html><head><meta charset="utf-8"><style>
/* （此处原样嵌入上方公共基座 CSS） */
.headline{font-size:{{headline_size}}px;font-weight:900;line-height:1.25;
  margin:auto 0;word-break:break-all;}
.headline em{font-style:normal;color:{{accent_color}};}
.sub{font-size:44px;line-height:1.6;color:#444;margin-bottom:40px;}
.bar{width:160px;height:16px;background:{{accent_color}};margin-bottom:48px;}
</style></head><body>
<div class="page">
  <div class="kicker">{{kicker}}</div>
  <div class="headline">{{headline}}</div>
  <div class="bar"></div>
  <div class="sub">{{sub}}</div>
  <div class="pageno">{{page_no}}</div>
</div>
</body></html>
```

**变量表**（写入任务 6 的内页版式库.md）：

| 变量 | 内页用法 | 封面（降级）用法 |
|---|---|---|
| `{{kicker}}` | 小节眉题 | 人群/场景钩子（如「熬夜党必看」） |
| `{{headline}}` | 核心观点，`<em>` 标利益点 | 封面大字（即视觉锚点，≤16 字） |
| `{{headline_size}}` | 88 | 120 |
| `{{sub}}` | 一句展开 | 副利益点或留空 |
| `{{page_no}}` | `02/07` 格式 | 留空 |
| `{{bg_color}}` `{{accent_color}}` | 取自账号档案候选风格的配色 | 同左，需强对比 |

- [ ] **步骤 2：实例化一份填好变量的副本渲染验证**

复制到 `06-产出/00000000-selftest/html/statement-test.html`，把全部 `{{变量}}` 替换为真实值（headline 用一句 14 字中文、`{{headline_size}}`=120、配色 `#FFF8F0`/`#E64A2E`），运行：

```powershell
node 04-工具/render.mjs 06-产出/00000000-selftest
```

预期：退出码 0，打开 `images/statement-test.png` 目检——大字清晰不糊、无溢出、缩略到 25% 仍可读 headline（这就是锚点判据①的自测法）。

- [ ] **步骤 3：Commit**

```powershell
git add 02-规范/templates/card-statement.html 06-产出/00000000-selftest/
git commit -m "feat: statement card template (doubles as fallback cover)"
```

---

### 任务 4：其余四个版式模板

**文件：** 创建 `02-规范/templates/card-checklist.html`、`card-compare.html`、`card-quote.html`、`card-cta.html`

每个模板：嵌入任务 3 的公共基座 CSS，再加各自专属部分。清单/对比模板的条目数固定占位（Agent 实例化时删除多余条目而非追加，防溢出）。

- [ ] **步骤 1：card-checklist.html（清单/步骤卡，最多 5 条）**

专属 CSS 与结构：

```html
<style>/* 公共基座 CSS + 以下 */
.title{font-size:72px;font-weight:900;margin:24px 0 56px;}
.item{display:flex;gap:32px;align-items:flex-start;margin-bottom:44px;}
.num{flex:none;width:72px;height:72px;border-radius:50%;background:{{accent_color}};
  color:#fff;font-size:40px;font-weight:bold;display:flex;align-items:center;justify-content:center;}
.item-title{font-size:46px;font-weight:bold;line-height:1.3;}
.item-desc{font-size:36px;color:#555;line-height:1.5;margin-top:8px;}
</style>
<div class="page">
  <div class="kicker">{{kicker}}</div>
  <div class="title">{{title}}</div>
  <div class="item"><div class="num">1</div><div><div class="item-title">{{item1_title}}</div><div class="item-desc">{{item1_desc}}</div></div></div>
  <div class="item"><div class="num">2</div><div><div class="item-title">{{item2_title}}</div><div class="item-desc">{{item2_desc}}</div></div></div>
  <div class="item"><div class="num">3</div><div><div class="item-title">{{item3_title}}</div><div class="item-desc">{{item3_desc}}</div></div></div>
  <div class="item"><div class="num">4</div><div><div class="item-title">{{item4_title}}</div><div class="item-desc">{{item4_desc}}</div></div></div>
  <div class="item"><div class="num">5</div><div><div class="item-title">{{item5_title}}</div><div class="item-desc">{{item5_desc}}</div></div></div>
  <div class="pageno">{{page_no}}</div>
</div>
```

- [ ] **步骤 2：card-compare.html（对比表卡，左右两栏各 4 行）**

```html
<style>/* 公共基座 CSS + 以下 */
.title{font-size:72px;font-weight:900;margin:24px 0 56px;}
.cols{display:flex;gap:32px;flex:1;}
.col{flex:1;border-radius:24px;padding:48px 40px;}
.col.bad{background:#F3F3F3;}
.col.good{background:{{accent_color}}18;border:4px solid {{accent_color}};}
.col-head{font-size:52px;font-weight:900;margin-bottom:40px;text-align:center;}
.col.bad .col-head{color:#888;}
.col.good .col-head{color:{{accent_color}};}
.row{font-size:38px;line-height:1.45;margin-bottom:36px;padding-left:44px;position:relative;}
.col.bad .row::before{content:"✗";position:absolute;left:0;color:#B00;}
.col.good .row::before{content:"✓";position:absolute;left:0;color:{{accent_color}};font-weight:bold;}
</style>
<div class="page">
  <div class="kicker">{{kicker}}</div>
  <div class="title">{{title}}</div>
  <div class="cols">
    <div class="col bad"><div class="col-head">{{left_head}}</div>
      <div class="row">{{left1}}</div><div class="row">{{left2}}</div><div class="row">{{left3}}</div><div class="row">{{left4}}</div></div>
    <div class="col good"><div class="col-head">{{right_head}}</div>
      <div class="row">{{right1}}</div><div class="row">{{right2}}</div><div class="row">{{right3}}</div><div class="row">{{right4}}</div></div>
  </div>
  <div class="pageno">{{page_no}}</div>
</div>
```

- [ ] **步骤 3：card-quote.html（金句引用卡）**

```html
<style>/* 公共基座 CSS + 以下 */
.quote-mark{font-size:200px;color:{{accent_color}};font-family:Georgia,serif;line-height:0.6;margin-top:80px;}
.quote{font-size:76px;font-weight:900;line-height:1.5;margin:64px 0 auto;}
.attribution{font-size:38px;color:#777;text-align:right;margin-bottom:40px;}
</style>
<div class="page">
  <div class="kicker">{{kicker}}</div>
  <div class="quote-mark">“</div>
  <div class="quote">{{quote}}</div>
  <div class="attribution">{{attribution}}</div>
  <div class="pageno">{{page_no}}</div>
</div>
```

- [ ] **步骤 4：card-cta.html（总结CTA卡）**

```html
<style>/* 公共基座 CSS + 以下 */
.title{font-size:80px;font-weight:900;margin:40px 0 64px;}
.recap{font-size:42px;line-height:1.7;color:#444;margin-bottom:24px;padding-left:40px;position:relative;}
.recap::before{content:"·";position:absolute;left:0;color:{{accent_color}};font-weight:900;font-size:56px;line-height:1.2;}
.cta{margin-top:auto;background:{{accent_color}};color:#fff;border-radius:24px;
  padding:48px 56px;font-size:48px;font-weight:bold;text-align:center;line-height:1.4;}
</style>
<div class="page">
  <div class="kicker">{{kicker}}</div>
  <div class="title">{{title}}</div>
  <div class="recap">{{recap1}}</div>
  <div class="recap">{{recap2}}</div>
  <div class="recap">{{recap3}}</div>
  <div class="cta">{{cta_text}}</div>
  <div class="pageno">{{page_no}}</div>
</div>
```

- [ ] **步骤 5：四模板各实例化一份渲染验证（规格 §11.2 前半）**

同任务 3 步骤 2 的方法：各复制一份到 selftest/html/ 填真实中文内容（清单填满 5 条、对比填满左右各 4 行——即最坏情况），运行 render.mjs。预期：4 张 PNG 全部退出码 0、无溢出、目检文字清晰。若任何模板最坏情况溢出，调小该模板字号/间距直至通过（这是模板的责任，不是内容的责任）。

- [ ] **步骤 6：Commit**

```powershell
git add 02-规范/templates/ 06-产出/00000000-selftest/
git commit -m "feat: four content card templates with worst-case render check"
```

---

### 任务 5：硬校验脚本 verify.mjs（TDD）

**文件：** 创建 `04-工具/verify.mjs`；坏例夹具在 selftest 运行目录

实现规格 §9 硬校验。零 npm 依赖（PNG 尺寸直接读 IHDR 头）。依赖两个数据约定（规格 §9 已定）：copy.md 顶部 ```json 块、风控清单.md 内 ```json 词表（任务 6 创建，本任务先用夹具）。

- [ ] **步骤 1：建夹具**

`06-产出/00000000-selftest/copy.md`：

````markdown
# 文案（自检夹具）

```json
{
  "titles": ["熬夜党的救命清单来了", "这份超长标题一定会超过二十个字符的限制吧", "睡前 3 件小事"],
  "recommended": 0,
  "body": "正文占位……（不超 1000 字）",
  "tags": ["#睡眠", "#自律", "#好物分享"],
  "comment_guide": "你最晚熬到几点？评论区聊聊"
}
```
````

（第 2 个标题 21 字——故意的坏例。）另放一张非 3:4 图进 `images/`：复制 good.png 后用以下命令造一张 1000×1000 的：

```powershell
node -e "import('puppeteer').then(async m=>{const b=await m.default.launch();const p=await b.newPage();await p.setViewport({width:1000,height:1000});await p.goto('about:blank');await p.screenshot({path:'06-产出/00000000-selftest/images/bad-ratio.png'});await b.close();})"
```

临时词表 `06-产出/00000000-selftest/banned.json`：`{"banned":["微信","私信我","V.","加我"]}`

- [ ] **步骤 2：运行不存在的脚本确认失败**

```powershell
node 04-工具/verify.mjs 06-产出/00000000-selftest
```

预期：Cannot find module。

- [ ] **步骤 3：实现 verify.mjs**

```javascript
// 04-工具/verify.mjs — 发布前硬校验（规格 §9）。零依赖。
// 用法: node 04-工具/verify.mjs <运行目录> [词表json路径，默认 02-规范/风控清单.md]
import { readFile, readdir, stat } from 'node:fs/promises';
import path from 'node:path';

const runDir = process.argv[2];
if (!runDir) { console.error('用法: node verify.mjs <运行目录>'); process.exit(2); }
const errors = [];

// --- 1. 解析 copy.md 的 json 块 ---
const copyRaw = await readFile(path.join(runDir, 'copy.md'), 'utf8');
const m = copyRaw.match(/```json\s*([\s\S]*?)```/);
if (!m) { console.error('copy.md 缺少 ```json 机器可读块'); process.exit(2); }
const copy = JSON.parse(m[1]);

// --- 2. 标题/正文/标签 ---
if (!Array.isArray(copy.titles) || copy.titles.length !== 3) errors.push('标题候选须恰好 3 个');
for (const t of copy.titles ?? []) if ([...t].length > 20) errors.push(`标题超 20 字: "${t}" (${[...t].length}字)`);
if ([...(copy.body ?? '')].length > 1000) errors.push(`正文超 1000 字 (${[...copy.body].length}字)`);
const nTags = (copy.tags ?? []).length;
if (nTags < 3 || nTags > 6) errors.push(`标签须 3-6 个，当前 ${nTags}`);
if (!copy.comment_guide) errors.push('缺评论区首条引导语');

// --- 3. 图片：数量、纯英文路径、3:4 比例（读 PNG IHDR） ---
const imgDir = path.join(runDir, 'images');
const pngs = (await readdir(imgDir)).filter(f => f.endsWith('.png'));
if (pngs.length < 2 || pngs.length > 9) errors.push(`图片须 2-9 张，当前 ${pngs.length}`);
for (const f of pngs) {
  if (!/^[a-z0-9._-]+$/i.test(f)) errors.push(`图片文件名含非英文字符: ${f}`);
  const buf = await readFile(path.join(imgDir, f));
  const w = buf.readUInt32BE(16), h = buf.readUInt32BE(20); // PNG IHDR
  const ratio = w / h;
  if (Math.abs(ratio - 3 / 4) > 0.02 * (3 / 4)) errors.push(`图片比例非 3:4 (±2%): ${f} (${w}x${h})`);
}
if (!pngs.includes('cover.png')) errors.push('缺 cover.png');

// --- 4. 风控词扫描（标题+正文+标签+引导语） ---
const wordSrc = process.argv[3] ?? path.join(import.meta.dirname, '..', '02-规范', '风控清单.md');
const wordRaw = await readFile(wordSrc, 'utf8');
const wm = wordRaw.match(/```json\s*([\s\S]*?)```/);
const banned = wm ? JSON.parse(wm[1]).banned : [];
const fullText = [...(copy.titles ?? []), copy.body, ...(copy.tags ?? []), copy.comment_guide].join('\n');
for (const w of banned) if (fullText.includes(w)) errors.push(`命中风控词: "${w}"`);

// --- 结果 ---
if (errors.length) { errors.forEach(e => console.error('FAIL ' + e)); process.exit(1); }
console.log(`PASS 硬校验全部通过（${pngs.length} 张图，标题 3 候选）`);
```

- [ ] **步骤 4：验证——坏例全被抓**

```powershell
node 04-工具/verify.mjs 06-产出/00000000-selftest 06-产出/00000000-selftest/banned.json
```

预期退出码 1，且报出两条：超长标题、bad-ratio.png 比例错误。随后修好夹具（换 ≤20 字标题、删 bad-ratio.png、补 statement-test.png 改名 cover.png——注意 selftest 的图是 2160×2880，比例合规），重跑预期 `PASS`。再往 copy.md 正文加「加我微信」重跑，预期报命中风控词——然后移除。

- [ ] **步骤 5：Commit**

```powershell
git add 04-工具/verify.mjs 06-产出/00000000-selftest/
git commit -m "feat: zero-dep hard-check script (titles/body/images/banned-words)"
```

---

### 任务 6：02-规范/ 六个规范文件

**文件：** 创建规格 §2 所列 `02-规范/` 下 6 个 .md。每个文件写完后逐条对照规格 §6.1-§6.6 的内容密度清单自查——那是验收线。以下给出每个文件的骨架与必备内容。

- [ ] **步骤 1：平台硬约束.md**

表格形式，7 行（标题字数/正文字数/图片数量/图片比例尺寸/路径规则/单端登录/发布频率），列：约束值 | 出处 | 校验方式（`verify.mjs` or 人工）| 违反后果。数值一律从规格 §6.1 抄，不自创。

- [ ] **步骤 2：封面锚点判据.md**

四部分：① 双变量模型定义（一段话，注明内化自 ponyo-cover-anchor-system 并附仓库链接）；② 6 模板各一小节（大字报/人物情绪/数字冲击/对比/清单/场景实拍——各含：适用内容类型、锚点是什么、提示词要点 3 条）；③ 选型映射表（内容类型→模板：干货教程→大字报或清单、情感共鸣→人物情绪、测评→对比、数据结论→数字冲击、生活方式→场景实拍）；④ **5 项过/不过诊断表**——逐字采用规格 §6.2 的五项，每项配一句「怎么测」（如①：把封面缩到 25% 看利益点是否可读）。

- [ ] **步骤 3：视觉风格库.md**

8 风格各一小节（规格 §6.3 清单），每节固定三字段：适用内容类型 / 提示词骨架（英文为主、可直接拼进生图提示词的一段话）/ 配色字体要点（给具体色值建议，供 HTML 模板变量取用）。末尾「黑名单」一节：蓝紫渐变、液态玻璃、塑料 3D 人物、油腻光效——写成可直接复制的英文负面提示词串。注明内化自 xhs-visual-director-skill。

- [ ] **步骤 4：内页版式库.md**

索引 + 变量表：5 个模板各一节，含适用职能（对应分镜表职能列）、变量表（任务 3/4 已定义，逐个变量说明取值来源）、条目上限（checklist≤5、compare 各≤4、recap≤3）与「删条目不加条目」的防溢出规则；card-statement 节注明兼任降级封面及两种用途的变量差异（任务 3 的表）。注明版式思路参考 guizang-social-card-skill。

- [ ] **步骤 5：文案规范.md**

四节：① 标题公式库——数字型/悬念型/身份共鸣型/反常识型各 2 个例句 + 「每篇出 3 候选、覆盖不同公式、标注推荐」的产出规则；② 正文三段结构（首行钩子→价值展开→提问结尾）+ 每段字数配比建议（1:7:2，总 ≤1000）；③ 标签策略（1 大流量 + 2 垂直 + 1 长尾，共 3-6 个）；④ 评论区首条引导模板 ≥3 条 + **加粗注明：引导语由用户发布后手动贴，Agent 不得调用评论工具**（规格 §6.5 原文）。

- [ ] **步骤 6：风控清单.md**

三节：① ```json 词表（`verify.mjs` 读取的机器格式）：

```json
{"banned":["微信","V.","VX","私信我","加我","公众号","QQ","二维码","绝对","最有效","第一名","根治","治愈","保证瘦","稳赚","躺赚"]}
```

② 敏感话题清单（医疗诊断建议、金融投资收益承诺、政治时事——写「规避方式：只谈个人体验不下结论」）；③ 行为红线（不自动互动、发布 ≤2 篇/天且间隔 ≥4h、不搬运）。

- [ ] **步骤 7：验证 verify.mjs 能读正式词表**

```powershell
node 04-工具/verify.mjs 06-产出/00000000-selftest
```

（不再传第三参数，走默认 `02-规范/风控清单.md`。）预期 `PASS`。

- [ ] **步骤 8：Commit**

```powershell
git add 02-规范/
git commit -m "feat: six spec files (constraints/cover-anchor/styles/layouts/copywriting/risk)"
```

---

### 任务 7：账号档案与总览文件

**文件：** 创建 `01-账号/账号档案.md`、`00-总览/术语与产物约定.md`、`00-总览/工作流总设计.md`

- [ ] **步骤 1：账号档案.md**

正文即规格 §7 的 YAML 块原样（version:1、mode:探索、candidate_domains:[] 等）+ 其下方三条规则原文（顺序轮换规则、收敛规则、首跑前置条件）。

- [ ] **步骤 2：术语与产物约定.md**

两部分：规格 §1 术语表原样抄录；规格 §4 产物 schema（运行目录树、run.yaml 全字段含状态枚举、已发档案 JSON 行、分镜表五列约束）原样抄录并对每个字段加一行人话解释。**这是断点续跑时 Agent 的快速参考，run.yaml 状态枚举与恢复语义（规格 §3 断点续跑规则）必须完整出现在本文件。**

- [ ] **步骤 3：工作流总设计.md**

仿照公众号项目同名文件的「索引 + 一图流」体例（参考 `../面向中年男性公众号选题思路/00-总览/工作流总设计.md`）：一句话定位、九节点一图流（规格 §3 表格的精简版：只留节点名+产物+确认点）、资产地图表（目录→作用→关键文件）、自动化边界四条（双确认、不自动互动、产物先落盘、降级不中断）。全文不超过 80 行——细则一律链接到规格，不重复。

- [ ] **步骤 4：Commit**

```powershell
git add 01-账号/ 00-总览/术语与产物约定.md 00-总览/工作流总设计.md
git commit -m "feat: account profile and overview docs"
```

---

### 任务 8：六个节点提示词

**文件：** 创建 `03-skills/prompts/` 下 6 个 .md

统一体例（沿用公众号项目 prompts 的 System/User变量/指令/输出格式 四段式）。每个文件必须写明：输入变量、必读规范文件、输出物及其落盘路径、通过标准（从规格 §3 对应行抄）、失败处理。以下给出每个文件的核心指令要点，执行者按四段式展开成文：

- [ ] **步骤 1：选题定角器.md**（节点③）

System：你是小红书选题策划。必读：`01-账号/账号档案.md`、本次 `research.md`。指令：产出角度陈述三要素（1 个明确角度 / 核心利益点 / 目标情绪），写入 storyboard.md 头部；探索模式下按档案轮换规则标注本篇 domain；research.md 为「未对标」时基于模型知识但在角度陈述中注明。输出格式：三要素 + domain 的 YAML 小块。通过标准：三要素齐备且互相一致。

- [ ] **步骤 2：分镜规划器.md**（节点④）

System：你是图文分镜师。必读：`02-规范/文案规范.md`、`02-规范/内页版式库.md`。指令：产出分镜表（封面 1 + 内页 4-7，五列见术语与产物约定.md），每页职能唯一、html-card 页注明模板名与变量值来源、ai-image 页给一句视觉描述；完读率设计：第 2 页必须承接封面钩子，最后一页必须是 CTA。输出：storyboard.md 完整内容。

- [ ] **步骤 3：封面导演.md**（节点⑤）

System：你是封面视觉导演。必读：`02-规范/封面锚点判据.md`、`02-规范/视觉风格库.md`、账号档案 candidate_styles。指令：按选型映射表选 6 模板之一并写选型理由 → 从候选风格选方向 → 产出 `prompts/cover-prompt.md`（完整正向提示词含锚点描述+尺寸 1080×1440+黑名单负面串+目标路径）→ 输出回填协议固定话术并暂停（run.yaml 置 waiting_backfill）。回填 2 轮不过或用户要求 → 降级：改用 card-statement 封面变量表生成 html，交 render.mjs 出图。

- [ ] **步骤 4：内页生成器.md**（节点⑥）

System：你是版式排版师。必读：内页版式库.md + 已确认的母版图风格。指令：逐页按分镜表实例化模板（复制模板到 `html/page-NN.html`、替换全部 {{变量}}、删除未用条目占位、配色变量从母版风格取）；全部页就绪后运行 `node 04-工具/render.mjs <运行目录>`；溢出报错时缩内容（不改模板）重渲染，超 2 轮问用户；ai-image 页走回填协议（page-NN-prompt.md）。

- [ ] **步骤 5：文案生成器.md**（节点⑦）

System：你是小红书文案。必读：文案规范.md、storyboard.md、research.md。指令：产出 copy.md——顶部 ```json 机器块（titles×3/recommended/body/tags/comment_guide，格式见规格 §9）+ 下方人读版；标题 3 候选覆盖不同公式并标注推荐；写完自跑 `node 04-工具/verify.mjs <运行目录>`，FAIL 项自动压缩重写，超 2 轮问用户。

- [ ] **步骤 6：发布器.md**（节点⑨）

System：你是发布执行员。前置：确认点 2 已 approved 且用户已选定标题。指令：调 xiaohongshu-mcp 的 publish_content（图片列表按 cover.png、page-01.png… 顺序、绝对路径、用户指定的定时参数）→ 成功则写 receipt.md（笔记 URL、发布时间、评论引导语文本 + 「请在 1 小时内手动贴到评论区」提醒）并追加已发档案.jsonl 一行（字段见术语与产物约定.md，metrics 全 null）→ **失败时先查本账号主页确认未发出**（未发出才重试 1 次；已发出视为成功补记；查询失败=状态不明，停止问人）。加粗红线：本 prompt 及全流程不得调用评论/点赞/收藏工具。

- [ ] **步骤 7：Commit**

```powershell
git add 03-skills/prompts/
git commit -m "feat: six node prompt files"
```

---

### 任务 9：主控 SKILL.md

**文件：** 创建 `03-skills/SKILL.md`

主控是运行时唯一入口，必须自包含以下八节（对照规格章节写，不发明新规则）：

- [ ] **步骤 1：写主控文件**

1. **触发与输入**：接受「主题」或「一段提示词」；生成 run 目录名 `YYYYMMDD-<英文slug>`（slug 由 Agent 从主题意译，纯英文小写短横线）。
2. **前置检查**：读账号档案（缺失或 candidate_domains/candidate_styles 为空 → 停止，引导用户填写）；调 check_login_status（未登录 → 停止提示扫码）。
3. **九节点编排**：规格 §3 表格原样嵌入，每节点注明用哪个 prompt 文件/脚本。
4. **断点续跑**：规格 §3「断点续跑规则」原文嵌入（含状态枚举、恢复语义、已批准确认点不重复、in_progress 整节点重跑）。
5. **确认点话术**：确认点 1 固定输出（封面图路径 + storyboard.md + 风格说明 + 5 项诊断自评表，问「通过还是调整？」）；确认点 2 固定输出（全部图片路径列表 + copy.md 人读版 + 3 标题候选让用户选 + 默认定时建议 20-22 点档，问「选哪个标题？现在发还是定时？」）。
6. **回填协议话术**：规格 §5 的固定话术模板。
7. **降级与失败路由**：规格 §3 各行失败处理的汇总表。
8. **红线**：不调用评论/点赞/收藏工具；发布失败先防重检查；产物先落盘。

- [ ] **步骤 2：走查验证**

用一个假想主题在文档上人肉走一遍九节点，确认每一步主控都能回答「读哪个文件、产出什么、卡住了怎么办」，无一处需要翻规格补课（主控要自包含）。发现缺口就补进主控。

- [ ] **步骤 3：Commit**

```powershell
git add 03-skills/SKILL.md
git commit -m "feat: master workflow SKILL.md with resume/confirm/degrade rules"
```

---

### 任务 10：验收文件与工具映射

**文件：** 创建 `05-验收/发布前检查清单.md`、`05-验收/已发档案.jsonl`、`04-工具/工具映射.md`

- [ ] **步骤 1：发布前检查清单.md**

两表：硬校验表（= verify.mjs 检查项逐条 + 运行命令 `node 04-工具/verify.mjs <运行目录>`）；软判据表（封面 5 项诊断引用封面锚点判据.md、整套风格一致、每页职能与分镜一致、文案与封面互补——各配「怎么检查」一句）。

- [ ] **步骤 2：已发档案.jsonl**

创建空文件（0 字节），并在 `05-验收/` 下建一个 `已发档案-字段说明.md`？——**不建**（YAGNI：字段说明已在术语与产物约定.md）。仅空 jsonl。

```powershell
New-Item -ItemType File "05-验收\已发档案.jsonl"
```

- [ ] **步骤 3：工具映射.md**

三节：① MCP 工具映射表（规格 §8.1 原样，含防重检查行与「实测时确认主页查询工具名」备注）；② 本地脚本（render.mjs / verify.mjs 用法与退出码：0=过、1=校验不过、2=用法/环境错）；③ 生图=回填协议（指向规格 §5，注明未来 MCP 升级路径）。

- [ ] **步骤 4：Commit**

```powershell
git add 05-验收/ 04-工具/工具映射.md
git commit -m "feat: acceptance checklist, archive file, tool mapping"
```

---

### 任务 11：薄壳触发器

**文件：** 创建 `C:\Users\Endercloud\.claude\skills\xhs-workflow\SKILL.md`

- [ ] **步骤 1：写薄壳**

```markdown
---
name: xhs-workflow
description: 小红书图文创作与发布工作流。当用户说 /xhs、「按小红书工作流创作…」、「用小红书工作流写…」或「继续 <run目录名>」（小红书笔记断点续跑）时触发。
---

# 小红书成稿工作流（触发器）

本文件只是入口。立即读取以下主控文件并严格按其执行，不要凭记忆复述流程：

`E:\Learning Files\Codex Project\General Codex Projects\小红书\03-skills\SKILL.md`

- 用户输入的主题/提示词原样传给主控的「触发与输入」节。
- 若用户意图是断点续跑（「继续 xxx」），先读对应 run.yaml，按主控「断点续跑」节恢复。
- 本文件不包含任何工作流逻辑；主控与本文件冲突时以主控为准。
```

- [ ] **步骤 2：触发验证（规格 §11.6）**

新开一个 Claude Code 会话，输入「/xhs 测试触发」，确认进入工作流（走到前置检查即算触发成功，可中止）。

- [ ] **步骤 3：Commit（薄壳在项目外，做个副本留档）**

把同样内容存一份到 `03-skills/薄壳触发器副本.md` 并 commit：

```powershell
git add 03-skills/薄壳触发器副本.md
git commit -m "feat: thin-shell trigger skill (copy; live file in ~/.claude/skills)"
```

---

### 任务 12：验收串测（规格 §11 逐条）

**文件：** 不新建（产出验收 run 目录）

- [ ] **步骤 1：§11.4 降级验证（先做，无需 MCP 在线）**

确认 xiaohongshu-mcp 服务未启动（或临时停掉），用薄壳触发一个测试主题走节点 ①②。预期：② 降级、research.md 写「未对标」、run.yaml 记 degradation、流程继续到 ③ 不中断。

- [ ] **步骤 2：§11.1 干跑测试 + §11.3a 回填坏例**

启动 xiaohongshu-mcp（运行项目根的 `xiaohongshu-mcp-windows-amd64.exe`），选一个真实测试主题完整走 ①→⑧（确认点 1、2 由你亲自把关；确认点 2 选 approved_unpublished 不发布）。在节点 ⑤ 回填时**故意先给一张错误比例图**：预期协议第 3 步拒绝并重新输出话术；再给合规图（或直接触发降级封面路线也算覆盖）。完成后检查：run 目录含规格 §4.1 全部产物、run.yaml 各节点状态合法、`verify.mjs` PASS。

- [ ] **步骤 3：§11.3 硬校验坏例回归**

对干跑产物依次注入三个坏例（21 字标题 / 1001 字正文 / 图片改中文名），各跑一次 verify.mjs 确认拦截，随后全部还原并重跑确认 PASS。

- [ ] **步骤 4：§11.2 渲染验收收尾**

确认 selftest 目录已有 5 模板的最坏情况渲染样张（任务 3/4 产物）；缺哪个补哪个。

- [ ] **步骤 5：§11.5 真发验证（需用户决策，不得自行执行）**

**此步必须由用户主动发起**：用户提供真实主题、亲自过双确认并说「发」。发布成功后检查 receipt.md 与已发档案.jsonl 记录正确、评论引导语提醒存在。此步完成前项目状态记为「搭建完成、待真发验收」。

- [ ] **步骤 6：收尾 commit + 汇报**

```powershell
git add -A
git commit -m "test: acceptance run artifacts (dry-run, degrade, bad-case regression)"
```

向用户汇报 §11 六条（+3a）逐条结果，注明 §11.5 待用户发起。

---

## 自检记录（编写计划后完成）

- **规格覆盖度**：§2 全部 23 个新文件均有任务归属（见文件结构总览表）；§3 九节点+断点续跑 → 任务 8/9；§4 schema → 任务 7（术语文件）+任务 8（各 prompt 落盘路径）；§5 回填协议 → 任务 8 步骤 3 + 任务 12 步骤 2；§6 六文件 → 任务 6；§7 → 任务 7；§8 → 任务 1/2/5/10；§9 → 任务 5；§10 → 任务 11；§11 → 任务 2/4/12 分摊，任务 12 汇总。
- **占位符扫描**：无「待定/TODO/类似任务N」；两个脚本与五个模板给出完整代码；六个规范文件与六个 prompt 给出的是内容清单而非代码——其"实际内容"即规格 §6/§3 的判据文本，任务步骤已指明逐条对照自查。
- **类型一致性**：`{{变量}}` 名在任务 3/4（模板）与任务 6 步骤 4（版式库变量表）一致；copy.md json 五字段在任务 5（verify.mjs）与任务 8 步骤 5（文案生成器）一致；run.yaml 状态枚举在任务 7 步骤 2 与任务 9 步骤 1 引用同一规格原文；脚本退出码约定（0/1/2）在任务 2/5/10 一致。
