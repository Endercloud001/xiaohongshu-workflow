# ponyo 封面生图融合 实现计划

> **面向 AI 代理的工作者：** 必需子技能：使用 superpowers:subagent-driven-development（推荐）或 superpowers:executing-plans 逐任务实现此计划。步骤使用复选框（`- [ ]`）语法来跟踪进度。

**目标：** 给小红书工作流封面节点⑤新增「产线乙 · ponyo AI 成品封面」，用 image-gen-mcp 直出含中文成品封面，规范化为精确 3:4，与现有 HTML 文字封面（产线甲）并存、可降级。

**架构：** 唯一新增运行时组件是 `04-工具/normalize-cover.mjs`（puppeteer 把任意比例 AI 图 `object-fit:cover` 裁成精确 1080×1440，零新依赖）。其余为封面导演 prompt 重写 + 4 处规范/文档增补说明。已验证脚本（render.mjs/verify.mjs）与 5 个 HTML 封面模板**不改**。ponyo skill 已项目级安装。

**技术栈：** Node.js ESM、puppeteer（项目已依赖）、Markdown 提示词/规范；image-gen-mcp（`mcp__image-gen-mcp__generate_image`，gpt-image-2-official）。

**规格：** [2026-07-03-ponyo封面生图融合-设计.md](2026-07-03-ponyo封面生图融合-设计.md)

**约定：** 运行目录 = `06-产出/<slug>/`。AI 原图存 `<运行目录>/assets/cover-src.png`（不进 `images/`）；规范化产物 `<运行目录>/images/cover.png`。所有脚本退出码契约对齐 render.mjs：`0`=成功 / `1`=校验不过 / `2`=用法或环境错误。

---

## 文件结构

| 文件 | 职责 | 动作 |
|---|---|---|
| `04-工具/normalize-cover.mjs` | AI 原图 → 精确 1080×1440(@2x=2160×2880) 3:4 封面 | 创建 |
| `04-工具/normalize-cover.test.mjs` | normalize 自测（正常路径尺寸 + 缺源图退出码） | 创建 |
| `03-skills/prompts/封面导演.md` | 封面节点 prompt，重写为产线甲/乙双路 | 重写 |
| `02-规范/封面锚点判据.md` | §③ 出图方式改「产线甲HTML/乙AI成品」；§④ 加 AI 封面中文正确性条件项 | 修改 |
| `04-工具/工具映射.md` | §3 生图通道改为已接入 image-gen-mcp + 规范化 + 降级链 | 修改 |
| `00-总览/工作流规格说明.md` | §5 回填协议改「AI 成品优先、HTML 兜底」 | 修改 |
| `03-skills/SKILL.md` | 主控注明双产线选型 + ponyo skill 封面专用（软约束） | 修改 |
| `.claude/skills/cover-anchor-system/**` | ponyo skill（已安装） | 提交（Task 7） |

---

## 任务 1：normalize-cover.mjs（AI 封面规范化组件，TDD）

**文件：**
- 创建：`04-工具/normalize-cover.mjs`
- 测试：`04-工具/normalize-cover.test.mjs`

- [ ] **步骤 1：编写失败的测试** — 创建 `04-工具/normalize-cover.test.mjs`

```js
// 04-工具/normalize-cover.test.mjs — normalize-cover.mjs 自测（造 fixture 用 puppeteer）
import puppeteer from 'puppeteer';
import { mkdir, rm, readFile, writeFile } from 'node:fs/promises';
import { spawnSync } from 'node:child_process';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const here = path.dirname(fileURLToPath(import.meta.url));
const tmp = path.join(here, '..', '06-产出', '00000000-selftest', 'normalize-tmp');
const assets = path.join(tmp, 'assets');
const images = path.join(tmp, 'images');
const fails = [];

// setup: 造一张 800x1200 (2:3，非3:4) 的 fixture → assets/cover-src.png
await rm(tmp, { recursive: true, force: true });
await mkdir(assets, { recursive: true });
{
  const b = await puppeteer.launch();
  const p = await b.newPage();
  await p.setViewport({ width: 800, height: 1200, deviceScaleFactor: 1 });
  await p.setContent('<div style="width:800px;height:1200px;background:#FF7A1A"></div>');
  await p.screenshot({ path: path.join(assets, 'cover-src.png') });
  await b.close();
}

// case 1: 正常路径 → images/cover.png 应恰为 2160x2880 (3:4)
{
  const r = spawnSync('node', [path.join(here, 'normalize-cover.mjs'), tmp], { encoding: 'utf8' });
  if (r.status !== 0) fails.push(`正常路径退出码应为0，实际${r.status}: ${r.stderr}`);
  try {
    const buf = await readFile(path.join(images, 'cover.png'));
    const w = buf.readUInt32BE(16), h = buf.readUInt32BE(20);
    if (w !== 2160 || h !== 2880) fails.push(`输出尺寸应为2160x2880，实际${w}x${h}`);
    // 容许 puppeteer 截图/DSF 缩放的浮点误差
    if (Math.abs(w / h - 3 / 4) > 0.02 * (3 / 4)) fails.push(`输出比例非3:4: ${w}x${h}`);
  } catch { fails.push('未生成 images/cover.png'); }
}

// case 2: 缺源图 → 退出码 2
{
  await rm(path.join(assets, 'cover-src.png'), { force: true });
  const r = spawnSync('node', [path.join(here, 'normalize-cover.mjs'), tmp], { encoding: 'utf8' });
  if (r.status !== 2) fails.push(`缺源图退出码应为2，实际${r.status}`);
}

// case 3: 坏源图（非 PNG 内容）→ 解码失败 → 退出码 1
{
  await mkdir(assets, { recursive: true });
  await writeFile(path.join(assets, 'cover-src.png'), 'not a real png');
  const r = spawnSync('node', [path.join(here, 'normalize-cover.mjs'), tmp], { encoding: 'utf8' });
  if (r.status !== 1) fails.push(`坏源图退出码应为1，实际${r.status}`);
}

await rm(tmp, { recursive: true, force: true });
if (fails.length) { fails.forEach(f => console.error('FAIL ' + f)); process.exit(1); }
console.log('PASS normalize-cover 自测通过'); process.exit(0);
```

- [ ] **步骤 2：运行测试验证失败**

运行：`node 04-工具/normalize-cover.test.mjs`
预期：非 0 退出，报错找不到 `normalize-cover.mjs`（`ERR_MODULE_NOT_FOUND` 或 spawn status 1）。

- [ ] **步骤 3：编写最少实现代码** — 创建 `04-工具/normalize-cover.mjs`

```js
// 04-工具/normalize-cover.mjs — AI 封面原图规范化为精确 1080×1440 (3:4)
// 用法: node 04-工具/normalize-cover.mjs <运行目录>
//   读 <运行目录>/assets/cover-src.png，用 object-fit:cover 居中裁切，
//   输出 <运行目录>/images/cover.png（2160×2880 = 1080×1440 @2x，恒 3:4）。
import puppeteer from 'puppeteer';
import { mkdir, readFile } from 'node:fs/promises';
import path from 'node:path';

const W = 1080, H = 1440, DSF = 2;
const runDir = process.argv[2];
if (!runDir) { console.error('用法: node normalize-cover.mjs <运行目录>'); process.exit(2); }
const src = path.resolve(runDir, 'assets', 'cover-src.png');
const imgDir = path.resolve(runDir, 'images');

// 以 data: URI 内联源图：headless Chrome 会拦截 setContent 文档里的 file:// 子资源
// （naturalWidth=0、decode 失败），故读进内存转 base64，避开跨源 file:// 限制。
let srcBuf;
try { srcBuf = await readFile(src); }
catch { console.error(`缺少源图: ${src}`); process.exit(2); }
await mkdir(imgDir, { recursive: true });

const dataUri = `data:image/png;base64,${srcBuf.toString('base64')}`;
const html = `<!DOCTYPE html><html><head><meta charset="utf-8"><style>
html,body{margin:0;padding:0;width:${W}px;height:${H}px;overflow:hidden;}
img{width:${W}px;height:${H}px;object-fit:cover;object-position:center;display:block;}
</style></head><body><img src="${dataUri}"></body></html>`;

let code = 0;
let browser;
try { browser = await puppeteer.launch(); }
catch (err) { console.error(`启动浏览器失败: ${err.message}`); process.exit(2); }
try {
  const page = await browser.newPage();
  await page.setViewport({ width: W, height: H, deviceScaleFactor: DSF });
  await page.setContent(html, { waitUntil: 'networkidle0' });
  // networkidle0 不保证 <img> 已解码渲染；显式等待 decode，失败即视为源图不可用
  const decoded = await page.evaluate(async () => {
    const img = document.querySelector('img');
    if (!img) return false;
    try { await img.decode(); } catch { return false; }
    return img.complete && img.naturalWidth > 0;
  });
  if (!decoded) {
    console.error(`源图无法解码/渲染（可能损坏或非图片）: ${src}`);
    code = 1;
  } else {
    await page.screenshot({ path: path.join(imgDir, 'cover.png') });
    console.log(`ok: cover.png (${W * DSF}x${H * DSF})`);
  }
} catch (err) {
  console.error(`规范化失败: ${err.message}`);
  code = 2;
} finally {
  try { await browser.close(); } catch {}
}
process.exit(code);
```

> **实现记录（2026-07-03）**：原 `file://`（`pathToFileURL`）方案在本机 headless Chrome 下会被跨源子资源限制拦截（`<img>` naturalWidth=0、decode 失败），改为 `data:` URI 内联并加 `img.decode()` 解码守卫（解码失败→退出码 1）。退出码契约：0=成功 / 1=源图不可解码 / 2=用法或浏览器启动失败。

- [ ] **步骤 4：运行测试验证通过**

运行：`node 04-工具/normalize-cover.test.mjs`
预期：`PASS normalize-cover 自测通过`，退出码 0。

- [ ] **步骤 5：Commit**

```bash
git add 04-工具/normalize-cover.mjs 04-工具/normalize-cover.test.mjs
git commit -m "feat: add normalize-cover.mjs (AI 封面规范化到精确 3:4)"
```

---

## 任务 2：重写封面导演为双产线

**文件：**
- 修改（整体替换）：`03-skills/prompts/封面导演.md`

- [ ] **步骤 1：用以下内容整体替换 `03-skills/prompts/封面导演.md`**

````markdown
# 封面导演（节点⑤ prompt）

## System

你是封面视觉导演。封面点击率是新号唯一生死线。本节点产出封面：**产线甲=HTML 文字封面**（确定性、中文锐利、零生图依赖）与**产线乙=ponyo AI 成品封面**（生图直出含字成品，视觉浑然一体）二选一，乙不达标可降级到甲。
必读：
- `../../02-规范/封面锚点判据.md`（双变量模型、6 模板选型映射表 §③、诊断 §④、HTML 模板变量表 §⑤）
- `../../02-规范/视觉风格库.md`（风格提示词骨架、黑名单负面串）
- `../../01-账号/账号档案.md` 的 `candidate_styles`
- 产线乙额外必读：`../../.claude/skills/cover-anchor-system/SKILL.md`（ponyo 选型矩阵、成品提示词方法）——**此 skill 仅本节点可用，其他节点不得调用**。

## User（变量）

- 运行目录：{{run_dir}}（已含 storyboard.md）

## 指令

1. 按封面锚点判据 §③ 选型映射表（内容类型→模板）选 1 型，写选型理由。
2. 从账号档案 candidate_styles 选风格方向（决定配色与生图风格骨架）。
3. **选产线（映射表已给首选，导演可据素材可得性调整）：**

**产线甲 · HTML 文字封面（文字主导型首选：大字报/数字冲击/清单/对比）**
   - 选对应模板（§⑤ 变量表）：大字报=`card-statement.html`、数字=`cover-number.html`、清单=`cover-list.html`、对比=`cover-compare.html`。
   - 复制到 `{{run_dir}}/html/cover.html`，填全部 `{{变量}}`（配色取自选定风格；大字报 `headline_size=120`、`headline≤16字`；数字 `number` 1-3 位；清单 `item≤4`；对比每侧一句话）。
   - `node 04-工具/render.mjs {{run_dir}}` 出 `images/cover.png`。溢出 FAIL→缩文字（不缩字号）重渲染，超 2 轮→换 §③ 备选模板或问用户。`run.yaml` tool 记 `html-cover`。

**产线乙 · ponyo AI 成品封面（图像主导型首选：人物情绪/场景实拍；任意型亦可选此路）**
   - **可选**调用 `cover-anchor-system` skill：按其模板矩阵与成品提示词方法，产出一条**完整成品封面提示词**（含准确中文文案、锚点位置、配色 HEX、字重、手绘/风格装饰）。skill 不可用时退回 `视觉风格库.md` 骨架自撰提示词。
   - 提示词固定包含：`3:4 vertical, 1080x1440`；「关键文字/数字置于中央安全区，远离上下边缘」；准确中文文案（照 storyboard 封面文字）；负面约束「no watermark, no @ handle, no logo, no QR code, no fake data, crisp correct Chinese, no garbled characters」。写入 `{{run_dir}}/prompts/cover-prompt.md`。
   - 调 `mcp__image-gen-mcp__generate_image`：`prompt`=上文、`size=auto`、`quality=high`。把返回 `image_url`(file://) 指向的 PNG **拷贝**到 `{{run_dir}}/assets/cover-src.png`。
   - `node 04-工具/normalize-cover.mjs {{run_dir}}` → 出精确 3:4 `images/cover.png`。
   - `run.yaml` tool 记 `ponyo-ai-cover`。

## 降级路线（产线乙不达标：确认点1 判中文乱码/构图差/含违规，或用户要求）

先重生成（换提示词 / 多打 2-3 张选一，各存 `assets/` 再挑一张拷成 `cover-src.png`）；仍不过 → 转产线甲：选与内容最贴近的 HTML 封面模板（情感/人物向优先 `card-statement.html` 大字报，把情绪浓缩成一句大字），填变量渲染。`run.yaml` 记 degradation（`ponyo-ai-cover`→`html-cover`）。

## 输出

- 产线甲：`{{run_dir}}/html/cover.html` + `images/cover.png`，tool 记 `html-cover`。
- 产线乙：`{{run_dir}}/prompts/cover-prompt.md` + `{{run_dir}}/assets/cover-src.png` + `images/cover.png`，tool 记 `ponyo-ai-cover`；降级后按上文记 degradation。

## 通过标准

选型理由已写；最终 `images/cover.png` 存在且过 3:4 尺寸校验；封面过封面锚点判据 §④ 诊断 ①-⑤（确认点 1 复核）。**产线乙额外**：过 §④ 的「⑥ 中文正确性」条件项（所有中文正确、无乱码、无多余英文/水印/杜撰）。

## 失败处理

- 产线甲渲染 FAIL → 缩文字重渲染，超 2 轮换备选模板或问用户。
- 产线乙 generate_image 失败/skill 缺失/规范化 FAIL → 直接走上方降级路线转产线甲。
````

- [ ] **步骤 2：验证**

运行：`grep -c "ponyo-ai-cover\|产线乙\|normalize-cover" "03-skills/prompts/封面导演.md"`
预期：≥3（三个关键串均出现）。

- [ ] **步骤 3：Commit**

```bash
git add 03-skills/prompts/封面导演.md
git commit -m "feat: 封面导演重写为产线甲/乙双路（HTML + ponyo AI 成品）"
```

---

## 任务 3：封面锚点判据 §③/§④ 增补

**文件：**
- 修改：`02-规范/封面锚点判据.md`

- [ ] **步骤 1：改 §③** — 把 §③ 开头「出图方式二选一」那段与映射表列名/取值中的「生图回填」全部改为「产线乙 ponyo AI 成品」，并在表格加产线维度。用以下内容替换 §③ 第 44 行说明段与其下映射表：

```markdown
**出图方式=产线二选一**：文字主导的 4 型（大字报/数字冲击/清单/对比）**首选产线甲 HTML→PNG 封面模板**（文字锚点 100% 可控、中文不糊、零生图依赖）；依赖真实图像或需浑然一体成品的 2 型（人物情绪/场景实拍）首选**产线乙 ponyo AI 成品封面**（image-gen-mcp 直出含字成品→`normalize-cover.mjs` 规范化 3:4）。**任意内容类型均可选产线乙**，由封面导演按素材与风格判断。

| 内容类型 | 首选模板 | 首选产线 | 备选 |
|---|---|---|---|
| 干货教程 | 大字报型 | 甲 HTML `templates/card-statement.html`（headline_size=120） | 乙 AI 成品 |
| 情感共鸣 | 人物情绪型 | **乙 ponyo AI 成品** | 甲 大字报（HTML） |
| 测评 / 对比 | 对比型 | 甲 HTML `templates/cover-compare.html` | 乙 AI 成品 |
| 数据结论 | 数字冲击型 | 甲 HTML `templates/cover-number.html` | 乙 AI 成品 |
| 盘点 / 步骤 | 清单型 | 甲 HTML `templates/cover-list.html` | 乙 AI 成品 |
| 生活方式 | 场景实拍型 | **乙 ponyo AI 成品** | 甲 大字报（HTML） |

> 走产线甲若渲染 FAIL/换风，按备选换 HTML 模板或转乙；走产线乙若中文/构图不达标，重生成或降级到最贴近的 HTML 模板（首选大字报 card-statement）。变量表见 §⑤。
```

- [ ] **步骤 2：改 §④** — 在 §④ 诊断表 5 行之后，追加一行 AI 封面条件诊断项：

```markdown
| ⑥ | 中文文字全部正确（**仅产线乙 AI 成品**） | | 逐字比对封面中文与 storyboard 封面文案：有乱码/错字/多余英文/水印/杜撰 logo → 不过，重生成或降级产线甲 |
```

并把 §④ 标题下说明「逐字采用规格 §6.2 五项」改为「①-⑤ 逐字采用规格 §6.2；⑥ 为产线乙 AI 成品专属条件项」。

- [ ] **步骤 3：验证**

运行：`grep -c "产线乙\|ponyo AI 成品\|中文文字全部正确" "02-规范/封面锚点判据.md"`
预期：≥3。

- [ ] **步骤 4：Commit**

```bash
git add 02-规范/封面锚点判据.md
git commit -m "docs: 封面锚点判据 §③ 出图方式改产线甲/乙，§④ 加 AI 封面中文正确性诊断"
```

---

## 任务 4：工具映射 §3 更新

**文件：**
- 修改：`04-工具/工具映射.md`

- [ ] **步骤 1：把 §2 脚本表补一行 normalize-cover，并整体替换 §3「生图通道」**

在 §2 脚本表（render.mjs / verify.mjs 之后）追加一行：

```markdown
| `04-工具/normalize-cover.mjs` | AI 封面原图 → 精确 1080×1440(2160×2880@2x) 3:4 | `node 04-工具/normalize-cover.mjs <运行目录>`（读 `assets/cover-src.png`，出 `images/cover.png`） | 0=成功 / 2=缺源图或环境错误 |
```

用以下内容整体替换 §3 全节：

```markdown
## 3. 生图通道（产线乙 · 已接入 image-gen-mcp）

封面产线乙走 `mcp__image-gen-mcp__generate_image`（模型 `gpt-image-2-official`，经 apimart 中转、异步补丁；配置见该 MCP 项目 `.env`）。

流程：封面导演出 `prompts/cover-prompt.md` → `generate_image(size=auto, quality=high)` → 返回 `image_url`(file://) 拷到 `<运行目录>/assets/cover-src.png`（**不进 `images/`**，避开 verify 逐张 3:4 校验与图片计数）→ `node 04-工具/normalize-cover.mjs <运行目录>` 规范化到精确 3:4 `images/cover.png`。

**降级链**：generate_image 失败 / ponyo skill 缺失 / normalize FAIL / 确认点1 判中文乱码或构图差 → 重生成 → 仍不过 → 转产线甲 HTML 文字封面（render.mjs，已验证兜底）。

**说明**：`size=auto` 让上游读提示词 3:4 线索，但比例不保证，故 normalize 步骤为强制、确定性归一化，不赌 auto。
```

- [ ] **步骤 2：验证**

运行：`grep -c "normalize-cover\|generate_image\|产线乙\|降级链" "04-工具/工具映射.md"`
预期：≥4。

- [ ] **步骤 3：Commit**

```bash
git add 04-工具/工具映射.md
git commit -m "docs: 工具映射接入 image-gen-mcp 生图通道 + normalize-cover + 降级链"
```

---

## 任务 5：工作流规格说明 §5 更新

**文件：**
- 修改：`00-总览/工作流规格说明.md`

- [ ] **步骤 1：定位 §5（回填协议 / 生图通道）**

运行：`grep -n "§5\|回填协议\|回填\|生图" "00-总览/工作流规格说明.md"`
读取该节现有内容，确定替换范围。

- [ ] **步骤 2：在 §5 节末追加以下说明段**（保留原回填协议描述，标注其为降级路径）

```markdown
**（v1.1 · 2026-07-03 更新）产线乙优先**：封面默认走产线乙自动生图（image-gen-mcp + ponyo skill）：出 `prompts/cover-prompt.md` → `generate_image` → `assets/cover-src.png` → `normalize-cover.mjs` → `images/cover.png`。上文的半自动人工回填协议降级为**兜底**（生图失败/无 MCP 时使用）；产线甲 HTML 文字封面为二级兜底。详见 [2026-07-03-ponyo封面生图融合-设计.md](2026-07-03-ponyo封面生图融合-设计.md)。
```

- [ ] **步骤 3：验证**

运行：`grep -c "产线乙优先\|normalize-cover\|兜底" "00-总览/工作流规格说明.md"`
预期：≥2。

- [ ] **步骤 4：Commit**

```bash
git add 00-总览/工作流规格说明.md
git commit -m "docs: 规格 §5 回填协议改为 AI 成品优先、HTML 兜底"
```

---

## 任务 6：主控 SKILL.md 注明双产线与 ponyo 作用域

**文件：**
- 修改：`03-skills/SKILL.md`

- [ ] **步骤 1：定位封面节点⑤段落**

运行：`grep -n "封面\|节点⑤\|节点 5\|⑤" "03-skills/SKILL.md"`
读取封面节点相关段，确定插入点。

- [ ] **步骤 2：在封面节点⑤描述处追加以下说明**

```markdown
> **封面双产线**：节点⑤有产线甲（HTML 文字封面，`render.mjs`）与产线乙（ponyo AI 成品封面，`image-gen-mcp` + `normalize-cover.mjs`），由封面导演按内容二选一，乙不达标降级甲。详见 `prompts/封面导演.md` 与 `../02-规范/封面锚点判据.md` §③。
> **ponyo skill 作用域**：`cover-anchor-system`（`.claude/skills/`）**仅封面节点⑤可调用**（软约束，harness 无硬开关），其他节点不得使用。
```

- [ ] **步骤 3：验证**

运行：`grep -c "封面双产线\|ponyo skill 作用域\|cover-anchor-system" "03-skills/SKILL.md"`
预期：≥2。

- [ ] **步骤 4：Commit**

```bash
git add 03-skills/SKILL.md
git commit -m "docs: 主控注明封面双产线与 ponyo skill 封面专用作用域"
```

---

## 任务 7：验收自测 + 提交 ponyo skill 与规格

**文件：**
- 提交：`.claude/skills/cover-anchor-system/**`、两份 `00-总览/2026-07-03-*.md`

- [ ] **步骤 1：normalize 自测复跑**

运行：`node 04-工具/normalize-cover.test.mjs`
预期：`PASS normalize-cover 自测通过`，退出码 0。

- [ ] **步骤 2：确认已验证脚本/模板未被改动**

运行：`git status --porcelain 04-工具/render.mjs 04-工具/verify.mjs 02-规范/templates/`
预期：**无输出**（这些文件零改动）。

- [ ] **步骤 3：确认 selftest 临时目录已清理**

运行：`ls "06-产出/00000000-selftest/normalize-tmp" 2>/dev/null; echo "exit=$?"`
预期：`exit=` 非 0（目录不存在，测试已自清理）。

- [ ] **步骤 4：提交 vendored skill + 规格 + 计划**

```bash
git add .claude/skills/cover-anchor-system 00-总览/2026-07-03-ponyo封面生图融合-设计.md 00-总览/2026-07-03-ponyo封面生图融合-实现计划.md
git commit -m "chore: vendored cover-anchor-system skill + ponyo 封面融合规格与计划"
```

---

## 任务 8：真实端到端干跑（人工验收，非代码）

> 此任务为运行时人工验收，需用户参与双确认，不产生代码 commit。

- [ ] **步骤 1：** 新起一篇（或复用测试选题），按主控走到节点⑤，选产线乙：出 `cover-prompt.md` → `generate_image` → `assets/cover-src.png` → `normalize-cover.mjs` → `images/cover.png`。
- [ ] **步骤 2：** 对该运行目录跑 `node 04-工具/verify.mjs <运行目录>`，预期封面比例校验通过（cover.png 存在且 3:4）。
- [ ] **步骤 3：** 确认点 1 人审：过封面锚点判据 §④ 的 ①-⑤ + ⑥ 中文正确性。
- [ ] **步骤 4：降级活体**：临时把 `cover-prompt.md` 的调用改为不可用（或断 MCP），验证封面导演按降级路线转产线甲 HTML 封面并过 verify。

---

## 自检记录

- **规格覆盖度**：设计 §2 双产线→任务2/3/6；§3 3:4 配方与规范化→任务1（normalize）+任务2（size=auto+拷贝流程）；§4 封面导演→任务2；§5 降级链→任务2/4；§6 人审中文正确性→任务3(§④⑥)+任务2(通过标准)；§7 ponyo 接入→已安装+任务6(作用域)+任务7(提交)；§8 文档清单→任务3/4/5/6；§9 新增组件→任务1；§10 验收→任务7/8；§11 YAGNI→计划未含内页配图/背景层/多风格。全覆盖。
- **占位符扫描**：无 TODO/待定；normalize 实现与测试均为完整代码；doc 任务均给出实际替换内容块与 grep 验证。
- **类型/命名一致性**：`assets/cover-src.png`、`images/cover.png`、tool 记号 `ponyo-ai-cover`/`html-cover`、退出码 0/1/2 在各任务间一致；`normalize-cover.mjs` 命名全篇统一。
