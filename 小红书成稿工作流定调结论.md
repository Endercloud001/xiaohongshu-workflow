# 小红书成稿工作流 · 定调结论

> 2026-07-02 头脑风暴会话产出，经用户三轮分节批准。本文件是后续「规格文档 → 实现计划 → 搭建」的定调依据，与 Claude 长期记忆（xiaohongshu-workflow-design）互为备份。
>
> 状态：**仅定调，尚未搭建**。除本文件与既有 MCP 文件外，`小红书/` 目录下其余结构均为规划，待实施。

## 一句话定调

在 `小红书/` 下建一套与公众号项目同构的「分层轻编辑部」：**通用创作引擎 + 可换账号档案**。输入一个主题（或一段提示词）→ 对标研究 → 选题定角 → 分镜 → 封面（AI 生图）→ 内页（HTML→PNG）→ 文案 → 双人工确认 → xiaohongshu-mcp 发布 → 归档并预留数据回流接口。

## 一、定调依据（五个指定资料 + 互联网调研）

### 指定资料结论

| 资料 | 在工作流中的角色 | 核心内容 |
|---|---|---|
| [xiaohongshu-mcp](https://github.com/xpzouying/xiaohongshu-mcp) | 发布与研究通道（已部署：`.mcp.json` 指向 http://localhost:18060/mcp，cookies 已登录） | 13 个 MCP 工具：发布图文/视频（支持定时）、搜索、推荐流、帖子详情、评论/点赞/收藏、登录管理 |
| [xhs-visual-director-skill](https://github.com/ziguishian/xhs-visual-director-skill) | 视觉方法论来源（裁剪内化） | 视觉导演三步法（判断任务→选风格→出方案）、24 种风格库、廉价 AI 风格黑名单、「先 1 张母版确认再出整套」机制、自检清单 |
| [ponyo-cover-anchor-system](https://github.com/ponyodong2026/ponyo-cover-anchor-system) | 封面方法论来源（裁剪内化），即波妞 X 长文的落地版 | 「信息密度 × 视觉锚点」双变量模型：封面 3 秒内要有眼睛停留焦点（人物/数字/强对比色块）且利益点一眼看清；6 种封面模板、诊断清单、提示词库，针对 3:4（1080×1440） |
| [云析 X 长文](https://x.com/yunxi0623/status/2067558749074751716)《我用 Codex 给自己搭了一个小红书工作流》 | 理念定调 | 做小红书最累的不是写一篇，而是每一篇都要重复：选题、起标题、做封面、排版、写标签、看数据——要固化成流水线，拒绝「刷到热点临时写」 |
| [波妞 X 长文](https://x.com/ponyodong/status/2068877783745613906)《视觉锚点才是小红书封面的杀手锏》 | 与 ponyo 仓库同源 | 研究几百张高点击封面得出锚点规律 |

### 平台硬约束（来自 xiaohongshu-mcp，写死进规范）

- 标题 ≤ 20 字；正文 ≤ 1000 字（发布前硬校验字数）
- 图片 3:4 比例（1080×1440），本地**纯英文路径**（避免中文路径），文件名用 `cover.png`、`page-01.png` 序列
- 同账号不允许多网页端登录（否则被踢出）
- 禁引流、禁纯搬运；新账号可能触发实名认证提醒（非封号）

### 算法与运营调研结论（质量判据的依据）

小红书是逐级流量池，关键阈值：

| 算法指标 | 阈值 | 工作流对应判据 |
|---|---|---|
| 封面点击率 | ≥ 4%（进下一流量池的生死线） | 封面锚点诊断（确认点 1 的核心） |
| 完读率 | ≥ 30% | 分镜叙事结构（每页职能明确、有钩子递进） |
| 互动率（赞+藏+评） | ≥ 5% | 评论区首条引导语设计 |
| 收藏/点赞比 | ≥ 30%（收藏权重高） | 内页信息卡的「值得收藏感」 |

其他运营事实：新号前 30 天搜索流量几乎为 0（正常，不焦虑），一切为推荐流服务；发布后 1 小时内回复评论有算法加权；最佳发布窗口 20–22 点；分享率比点赞更被算法加权。

### 同类项目参考

- [guizang-social-card-skill](https://github.com/op7418/guizang-social-card-skill)：HTML→PNG 组图路线代表（28 版式 10 主题），内页生产路线的思路来源
- 行业共识：① 自动化止步于「人工放行」，批量互动/无脑自动发是风控红线；② 真正的闭环是数据回流选题；③ 「Codex+选题表+模板批量套图」流水线已被验证可行

## 二、已锁定的五个关键决策（用户逐一选定）

1. **账号定位**：通用引擎 + 可换「账号档案」层。工作流不绑定领域，账号定位/人设/风格作为可配置档案注入。
2. **图片路线**：混合——封面走 AI 生图（锚点系统 + 风格库出提示词），内页走 HTML→PNG 信息卡（文字 100% 可控、零生图成本）。按页面职能选工具。
3. **人工确认点**：双确认——① 视觉母版确认（封面 + 分镜表，获批再生成整套，避免整套返工）；② 发布前放行（完整预览包确认后才调发布工具）。与公众号「草稿箱待人工放行」理念一致；xiaohongshu-mcp 是直接发布无草稿缓冲，所以放行点必须在调用之前。
4. **范围边界**：第一版 = 创作 + 对标研究（写之前用 MCP 搜同题材爆款）；数据回流不做实现，但归档格式预留字段。
5. **账号现状**：新号、领域未定 → 账号档案先写「探索模式」。

## 三、目录规划（批准稿）

```
小红书/
├── .mcp.json、*.exe、cookies.json        # 已有，发布通道，不动
├── 小红书成稿工作流定调结论.md            # 本文件
├── 00-总览/
│   ├── 工作流总设计.md                    # 入口索引 + 一图流
│   └── 术语与产物约定.md                  # 「笔记包」「母版图」「分镜表」等术语
├── 01-账号/
│   └── 账号档案.md                        # 定位/人设/领域/受众/风格倾向（可换、可多份）
├── 02-规范/
│   ├── 平台硬约束.md                      # 字数/尺寸/路径/风控红线（硬校验项）
│   ├── 封面锚点判据.md                    # 内化 ponyo：双变量模型 + 6 模板选型 + 诊断清单
│   ├── 视觉风格库.md                      # 内化 visual-director：裁剪 6-8 种风格 + 廉价AI风格黑名单
│   ├── 内页版式库.md                      # 内化 guizang 思路：自建 4-6 个 HTML 信息卡版式
│   ├── 文案规范.md                        # 标题公式/正文结构/标签策略/评论区首条引导
│   └── 风控清单.md                        # 禁引流、禁纯搬运、频率控制、敏感词
├── 03-skills/
│   ├── SKILL.md                          # 主控工作流（唯一入口）
│   └── prompts/                          # 节点提示词：选题定角器/分镜规划器/封面导演/
│                                         #   内页生成器/文案生成器/发布器
├── 04-工具/
│   └── 工具映射.md                        # MCP 工具清单、生图通道、HTML→PNG 渲染、降级规则
├── 05-验收/
│   ├── 发布前检查清单.md                  # 终审判据（硬校验 + 软判据）
│   └── 已发档案.jsonl                     # 归档（预留曝光/点赞/收藏/评论等数据回流字段）
└── 06-产出/
    └── <日期-主题>/                       # 每次运行：run.yaml、分镜.md、images/、文案.md、发布回执
```

设计纪律（沿用公众号项目）：

- 每个规范文件必须是**可执行判据**（能回答「过/不过」），不写理念散文
- 外部三项目只进 `02-规范/` 不进 skills：裁剪内化、注明出处，原仓库更新与否不影响运行

## 四、主流程（批准稿）

```
输入（主题 or 一段提示词）
   ↓
① 档案加载        读取账号档案 + 平台硬约束，确定本次创作边界
   ↓
② 对标研究        MCP 搜索同题材笔记 → 摘要爆款标题结构/封面套路/评论区高频需求
   ↓                （MCP 失败 → 降级跳过，用模型知识定角，产物标注「未对标」）
③ 选题定角        结合对标产出 1 个明确角度 + 核心利益点 + 目标情绪
   ↓
④ 分镜规划        输出「笔记分镜表」：封面 1 页 + 内页 4-7 页，每页一句话职能 +
   ↓               页面类型标记（AI生图页 / HTML信息卡页）+ 标题/正文/标签初稿
⑤ 封面生产        锚点判据选模板 → 风格库选方向 → 生成封面提示词 → AI 生图
   ↓
★ 确认点 1：视觉母版确认
   封面图 + 分镜表 + 风格说明一起给用户看。
   不过 → 回到 ④ 或 ⑤ 重做（最多 2 轮，仍不过则暂停待人工指示）
   ↓
⑥ 内页生产        HTML 版式库套模板 → 渲染 3:4 PNG；穿插氛围图走生图，风格锚定母版
   ↓
⑦ 文案定稿        标题（≤20字硬校验）、正文（≤1000字硬校验）、标签、评论区首条引导语
   ↓
⑧ 组包终审        按发布前检查清单自检 → 完整预览包落盘至 06-产出/<日期-主题>/
   ↓
★ 确认点 2：发布放行
   用户看完整包（所有图 + 文案 + 标签）明确说发才调 MCP 发布；
   可指定定时发布（默认建议 20-22 点档）
   ↓
⑨ 发布与归档      调 publish 工具 → 回执（笔记 URL）写入已发档案.jsonl（预留数据字段）
```

### 错误处理原则（「降级不中断」）

- 研究类节点（②）失败 → 降级跳过，标注即可
- 生产类节点（⑤⑥）失败 → 重试，超 2 轮暂停问人
- 发布节点（⑨）失败 → 原地重试一次，再失败报错停止，**绝不静默丢稿**
- 产物永远先落盘再发布：发布失败不损失任何工作

## 五、外部项目内化取舍表

| 来源 | 取 | 舍 |
|---|---|---|
| ponyo 锚点系统 | 双变量模型、6 模板选型逻辑、诊断清单（改写成封面验收判据） | 依赖 ChatGPT Image 的提示词措辞（按自己的生图通道重写） |
| visual-director | 风格判断三步法、母版确认机制、廉价 AI 风格黑名单、自检清单 | 24 风格全库（裁到 6-8 种）；每次 10-20 个苏格拉底提问（信息不足最多问 3 个，其余从账号档案取默认值） |
| guizang 版式思路 | HTML→PNG 内页生产路线、版式系统化思想 | 28 版式整包（第一版自建 4-6 个内页版式即够） |

## 六、运营定调（写进账号档案与总设计）

- **冷启动认知**：前 30 天搜索流量为 0 属正常，一切为推荐流服务，封面点击率是唯一生死线——这就是封面居于流程中心、占一个确认点的原因。
- **节奏**：起步隔日更或每周 3 篇（不追日更）；发布默认 20–22 点档。
- **人的作业（不自动化）**：发布后 1 小时内人工回复评论（算法加权；自动互动是风控红线，写进清单但不做自动化）。
- **探索模式 → 深耕模式**：领域未定，账号档案先写「探索模式」——前 9-12 篇允许在 2-3 个候选领域轮换试题，用已发档案.jsonl 的表现决定收敛方向；收敛后档案改「深耕模式」。这是数据回流接口的第一个用例。

## 七、验收标准与待验证依赖

**发布前检查清单：**

- 硬校验项：标题字数、正文字数、图片数量与 3:4 比例、本地英文路径、风控词扫描
- 软判据：封面锚点诊断（对照 02-规范/封面锚点判据.md）、整套图风格一致性

**实施时需验证的两个依赖：**

1. HTML→PNG 渲染工具：拟用 Playwright/Puppeteer 截图（公众号项目 node_modules 已有相关生态，实施时确认可用性）
2. 生图通道：沿用公众号工作流已验证的生图能力，实施时确认调用方式

## 八、下一步

1. 将本定调写成正式规格文档（建议放 `00-总览/`），走 writing-plans 出实现计划
2. 按计划搭建（预计 15-20 个文件），搭建中先验证上述两个依赖
3. 首次运行前：在单独对话（或工作流「探索模式」首跑）中确定 2-3 个候选领域，写入第一份账号档案
